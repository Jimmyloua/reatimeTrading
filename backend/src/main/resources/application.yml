server:
  port: 8080

spring:
  application:
    name: trading-platform

  datasource:
    url: jdbc:mysql://localhost:3306/trading_platform?createDatabaseIfNotExist=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml

  data:
    redis:
      host: localhost
      port: 6379
      password:

  threads:
    virtual:
      enabled: true

jwt:
  secret: "REPLACE_WITH_GENERATED_BASE64_SECRET_MIN_32_CHARS"
  access-token-expiration: 900000
  refresh-token-expiration: 604800000

logging:
  level:
    com.tradingplatform: INFO

avatar:
  upload-dir: ./uploads/avatars
  max-size: 5242880  # 5 MB
