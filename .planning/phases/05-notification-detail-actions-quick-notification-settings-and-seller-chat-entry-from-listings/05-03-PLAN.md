---
phase: 05-notification-detail-actions-quick-notification-settings-and-seller-chat-entry-from-listings
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - frontend/src/pages/ListingDetailPage.tsx
  - frontend/src/tests/listing-chat-entry.test.tsx
autonomous: false
requirements:
  - CHAT-06
must_haves:
  truths:
    - "An authenticated buyer can start or resume the seller conversation directly from listing detail and land in the correct thread."
    - "The listing owner cannot start a chat with themselves from listing detail."
    - "Phase 5 critical user flows have a final manual confirmation gate for routing and discoverability."
  artifacts:
    - path: "frontend/src/pages/ListingDetailPage.tsx"
      provides: "Seller chat entry from listing detail"
    - path: "frontend/src/tests/listing-chat-entry.test.tsx"
      provides: "Automated proof for seller-chat entry behavior"
  key_links:
    - from: "frontend/src/pages/ListingDetailPage.tsx"
      to: "frontend/src/api/chatApi.ts"
      via: "createConversation({ listingId })"
      pattern: "createConversation"
    - from: "frontend/src/pages/ListingDetailPage.tsx"
      to: "frontend/src/pages/MessagesPage.tsx"
      via: "navigate('/messages?conversation={id}')"
      pattern: "conversation="
---

<objective>
Expose seller chat entry from listing detail and close the phase with focused human verification.

Purpose: The backend and routing work are only valuable if the listing-detail surface can reuse them cleanly for buyers without reopening self-chat or navigation confusion.
Output: Listing detail seller-chat CTA wired to the existing conversation contract plus a blocking verification checkpoint.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-notification-detail-actions-quick-notification-settings-and-seller-chat-entry-from-listings/05-RESEARCH.md
@frontend/src/pages/ListingDetailPage.tsx
@frontend/src/api/chatApi.ts
@frontend/src/pages/MessagesPage.tsx
@frontend/src/tests/listing-chat-entry.test.tsx
@.planning/phases/04-transactions-and-trust/04-07-SUMMARY.md

<interfaces>
From `frontend/src/api/chatApi.ts`:
```ts
createConversation(request: { listingId: number; initialMessage?: string }): Promise<Conversation>
```

From `frontend/src/pages/ListingDetailPage.tsx` existing context:
```ts
const isOwner = user && listing && user.id === listing.seller.id
const listingId = parseInt(id || '0', 10)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add seller chat CTA on listing detail per CHAT-06</name>
  <read_first>
    - frontend/src/pages/ListingDetailPage.tsx
    - frontend/src/api/chatApi.ts
    - frontend/src/tests/listing-chat-entry.test.tsx
    - .planning/phases/05-notification-detail-actions-quick-notification-settings-and-seller-chat-entry-from-listings/05-RESEARCH.md
  </read_first>
  <files>frontend/src/pages/ListingDetailPage.tsx, frontend/src/tests/listing-chat-entry.test.tsx</files>
  <acceptance_criteria>
    - Listing detail shows a seller-chat CTA only when the viewer is authenticated and `user.id !== listing.seller.id`.
    - Clicking the CTA calls `chatApi.createConversation({ listingId })` and navigates to `/messages?conversation={conversation.id}`.
    - Existing transaction initiation UI from Phase 4 remains intact; this CTA is an additional seller-contact path, not a replacement for transaction flow.
    - No seller-chat entry is added to browse cards or listing summaries, matching the Phase 5 research scope limit.
  </acceptance_criteria>
  <action>Add a concise seller-contact button or link inside the seller information card on `ListingDetailPage.tsx`, near the seller avatar/profile affordance, and wire it to the existing idempotent `chatApi.createConversation` contract. Preserve the current owner logic and keep self-chat blocked in the UI by hiding or disabling the CTA for the owner. Scope this work to listing detail only; do not expand browse card/list APIs or add seller summary fields to listing search responses.</action>
  <verify>
    <automated>npm test -- --run listing-chat-entry.test.tsx</automated>
  </verify>
  <done>Buyers can start or resume the seller chat directly from listing detail with passing automated coverage, and owners do not see a self-chat path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 5 notification and listing-entry flows end to end</name>
  <read_first>
    - .planning/phases/05-notification-detail-actions-quick-notification-settings-and-seller-chat-entry-from-listings/05-VALIDATION.md
    - frontend/src/pages/ListingDetailPage.tsx
    - frontend/src/pages/MessagesPage.tsx
    - frontend/src/components/notifications/NotificationDropdown.tsx
  </read_first>
  <files>frontend/src/pages/ListingDetailPage.tsx, frontend/src/pages/MessagesPage.tsx, frontend/src/components/notifications/NotificationDropdown.tsx</files>
  <acceptance_criteria>
    - A buyer can open a listing, start/resume chat from seller info, and land on the correct thread.
    - Clicking a `NEW_MESSAGE` notification opens the correct conversation and unread state clears immediately.
    - Quick settings toggles are discoverable from notification surfaces and persist after refresh.
  </acceptance_criteria>
  <action>Claude should complete all automation first, then pause for a focused human flow check because the remaining risk is UX clarity and route correctness across real browser navigation.</action>
  <verify>
    <automated>npm test -- --run listing-chat-entry.test.tsx notification-actions.test.tsx notification-preferences.test.tsx messages-page-routing.test.tsx</automated>
  </verify>
  <what-built>Listing-detail seller chat entry, notification detail actions, quick notification settings, and URL-backed message bootstrap.</what-built>
  <how-to-verify>
    1. Log in as a non-owner, open a listing detail page, and confirm the seller card shows the chat CTA.
    2. Click the CTA and confirm the app lands on `/messages?conversation={id}` with the intended conversation selected.
    3. Trigger or seed a `NEW_MESSAGE` notification, click it from the bell dropdown or notifications page, and confirm the same conversation opens with unread badges cleared.
    4. Open notification quick settings, disable one category, refresh, and confirm the toggle state persists.
    5. Log in as the listing owner and confirm the seller-chat CTA is hidden or blocked.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase or describe the failing step.</resume-signal>
  <done>Human verification confirms the Phase 5 flows behave correctly in the running application.</done>
</task>

</tasks>

<verification>
Use the automated listing-entry test first, then complete the blocking human verification checklist before treating Phase 5 as shippable.
</verification>

<success_criteria>
Listing detail becomes a valid seller-chat entry surface without scope creep to listing cards, and the full notification-to-chat flow is confirmed by human verification.
</success_criteria>

<output>
After completion, create `.planning/phases/05-notification-detail-actions-quick-notification-settings-and-seller-chat-entry-from-listings/05-03-SUMMARY.md`
</output>
