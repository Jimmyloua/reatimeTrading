---
phase: 05-notification-detail-actions-quick-notification-settings-and-seller-chat-entry-from-listings
plan: 02
type: execute
wave: 2
depends_on:
  - 05-00
  - 05-01
files_modified:
  - frontend/src/api/notificationApi.ts
  - frontend/src/api/chatApi.ts
  - frontend/src/types/notification.ts
  - frontend/src/stores/notificationStore.ts
  - frontend/src/stores/chatStore.ts
  - frontend/src/hooks/useChat.ts
  - frontend/src/components/notifications/NotificationItem.tsx
  - frontend/src/components/notifications/NotificationDropdown.tsx
  - frontend/src/components/notifications/NotificationList.tsx
  - frontend/src/pages/NotificationsPage.tsx
  - frontend/src/pages/MessagesPage.tsx
  - frontend/src/tests/notification-actions.test.tsx
  - frontend/src/tests/messages-page-routing.test.tsx
autonomous: true
requirements:
  - NOTF-05
  - NOTF-06
  - CHAT-07
must_haves:
  truths:
    - "Clicking a notification opens the referenced conversation, listing, or transaction detail."
    - "The notification dropdown/page shows persisted recent notifications instead of only in-session pushes."
    - "The Messages page can open a specific conversation from the URL and clears stale unread state when that conversation is selected."
  artifacts:
    - path: "frontend/src/pages/MessagesPage.tsx"
      provides: "URL-backed conversation bootstrap for /messages?conversation={id}"
    - path: "frontend/src/components/notifications/NotificationItem.tsx"
      provides: "Notification action handling"
    - path: "frontend/src/api/notificationApi.ts"
      provides: "Preferences read/update API methods and history hydration"
    - path: "frontend/src/stores/chatStore.ts"
      provides: "Unread sync for inactive conversations"
  key_links:
    - from: "frontend/src/components/notifications/NotificationItem.tsx"
      to: "frontend/src/pages/MessagesPage.tsx"
      via: "navigate('/messages?conversation={id}')"
      pattern: "conversation="
    - from: "frontend/src/pages/MessagesPage.tsx"
      to: "frontend/src/api/chatApi.ts"
      via: "getConversation(id) when target thread is not already loaded"
      pattern: "getConversation"
    - from: "frontend/src/hooks/useChat.ts"
      to: "frontend/src/stores/chatStore.ts"
      via: "inactive conversation preview/unread updates"
      pattern: "incrementUnread|clearUnread"
---

<objective>
Implement Phase 5 notification actions, quick settings UI wiring, and deep-linkable message selection.

Purpose: Users need notifications to behave like entry points, not dead-end history items, and the messages page must honor external navigation from notifications and listing flows.
Output: Notification action resolver, persisted preference UI integration, dropdown hydration, URL-backed messages bootstrap, and chat unread synchronization.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-notification-detail-actions-quick-notification-settings-and-seller-chat-entry-from-listings/05-RESEARCH.md
@frontend/src/api/notificationApi.ts
@frontend/src/components/notifications/NotificationDropdown.tsx
@frontend/src/components/notifications/NotificationList.tsx
@frontend/src/pages/MessagesPage.tsx
@frontend/src/hooks/useChat.ts
@frontend/src/stores/chatStore.ts
@frontend/src/pages/TransactionDetailPage.tsx

<interfaces>
From `frontend/src/api/chatApi.ts`:
```ts
createConversation(request: { listingId: number; initialMessage?: string }): Promise<Conversation>
getConversations(page?: number, size?: number): Promise<{ content: Conversation[]; totalElements: number }>
getConversation(id: number): Promise<Conversation>
getMessages(conversationId: number, page?: number, size?: number): Promise<{ content: Message[]; totalElements: number }>
```

From `frontend/src/types/notification.ts`:
```ts
interface Notification {
  id: number
  type: 'NEW_MESSAGE' | 'ITEM_SOLD' | 'TRANSACTION_UPDATE' | 'SYSTEM_ANNOUNCEMENT' | 'PAYMENT_STATUS'
  referenceId?: number
  referenceType?: string
  read: boolean
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire notification actions and quick settings UI per NOTF-05 and NOTF-06</name>
  <read_first>
    - frontend/src/components/notifications/NotificationDropdown.tsx
    - frontend/src/components/notifications/NotificationList.tsx
    - frontend/src/api/notificationApi.ts
    - frontend/src/types/notification.ts
    - frontend/src/tests/notification-actions.test.tsx
  </read_first>
  <files>frontend/src/api/notificationApi.ts, frontend/src/types/notification.ts, frontend/src/stores/notificationStore.ts, frontend/src/components/notifications/NotificationItem.tsx, frontend/src/components/notifications/NotificationDropdown.tsx, frontend/src/components/notifications/NotificationList.tsx, frontend/src/pages/NotificationsPage.tsx, frontend/src/tests/notification-actions.test.tsx</files>
  <acceptance_criteria>
    - `notificationApi` adds `getPreferences()` and `updatePreferences()` methods matching the backend DTO names from Plan 05-01.
    - Notification dropdown and notifications page hydrate from persisted notification history, not only WebSocket session state.
    - `NotificationItem` click behavior resolves `NEW_MESSAGE` to `/messages?conversation={referenceId}`, `ITEM_SOLD` to `/listings/{referenceId}`, and `TRANSACTION_UPDATE` to `/transactions/{referenceId}`.
    - Quick settings expose toggles for `newMessageEnabled`, `itemSoldEnabled`, and `transactionUpdateEnabled` from the bell dropdown and/or notifications page without redesigning the notification center.
  </acceptance_criteria>
  <action>Expand notification types/API to include the preference DTO and add a compact quick-settings surface reachable from `NotificationDropdown`, plus persisted controls on `NotificationsPage` if needed for clarity. Reuse current notification components instead of introducing a new subsystem. Implement action routing from `type + referenceType + referenceId`; never infer destinations from title/content strings. When a notification is opened, mark it as read through the existing REST endpoint before or during navigation so unread counts stay consistent.</action>
  <verify>
    <automated>npm test -- --run notification-actions.test.tsx</automated>
  </verify>
  <done>Notification UI supports persisted quick settings, recent-history hydration, and reliable route actions with passing notification action tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make MessagesPage URL-backed and keep unread state synchronized per CHAT-07</name>
  <read_first>
    - frontend/src/pages/MessagesPage.tsx
    - frontend/src/hooks/useChat.ts
    - frontend/src/stores/chatStore.ts
    - frontend/src/api/chatApi.ts
    - frontend/src/tests/messages-page-routing.test.tsx
  </read_first>
  <files>frontend/src/api/chatApi.ts, frontend/src/stores/chatStore.ts, frontend/src/hooks/useChat.ts, frontend/src/pages/MessagesPage.tsx, frontend/src/tests/messages-page-routing.test.tsx</files>
  <acceptance_criteria>
    - `MessagesPage` reads `conversation` from `useSearchParams`, validates it as a number, and uses it as the source of truth for the selected thread.
    - When the requested conversation is not already in `chatStore.conversations`, the page fetches it via `chatApi.getConversation(id)` and inserts/selects it.
    - Selecting a conversation updates the URL query param instead of only local component state.
    - Incoming WebSocket messages update inactive conversation preview/unread metadata, and selecting/opening a conversation clears its local unread badge immediately.
  </acceptance_criteria>
  <action>Refactor `MessagesPage` away from local-only `useState` selection to URL-backed selection using `react-router-dom` search params. Extend `chatStore` with helpers to upsert conversations and refresh preview metadata from incoming messages. Update `useChat` so `/user/queue/messages` traffic updates store state for both active and inactive conversations; only append to the open message list when the message belongs to the active thread. Reuse existing `/api/conversations/{id}` and `/api/conversations/{id}/messages` endpoints; do not add a new chat bootstrap route.</action>
  <verify>
    <automated>npm test -- --run messages-page-routing.test.tsx</automated>
  </verify>
  <done>The messages page can be deep-linked into a specific conversation, and unread badges no longer drift when messages arrive outside the active thread.</done>
</task>

</tasks>

<verification>
Run the targeted frontend tests plus a quick app build if needed to catch router/type regressions caused by search-param and store changes.
</verification>

<success_criteria>
Notifications become actionable navigation surfaces, quick settings persist through the API, and `/messages` behaves correctly when entered from outside the chat page.
</success_criteria>

<output>
After completion, create `.planning/phases/05-notification-detail-actions-quick-notification-settings-and-seller-chat-entry-from-listings/05-02-SUMMARY.md`
</output>
