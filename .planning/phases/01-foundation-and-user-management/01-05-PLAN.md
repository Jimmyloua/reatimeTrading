---
phase: 01-foundation-and-user-management
plan: 05
type: execute
wave: 4
depends_on: [01-03-PLAN.md, 01-04-PLAN.md]
files_modified: [frontend/src/pages/ProfilePage.tsx, frontend/src/pages/UserProfilePage.tsx, frontend/src/components/AvatarUpload.tsx, frontend/src/components/ProfilePrompt.tsx, frontend/src/api/userApi.ts, frontend/src/App.tsx]
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03, PROF-04]
user_setup: []

must_haves:
  truths:
    - "User can view and edit their profile with display name"
    - "User can upload avatar via drag-and-drop or file picker"
    - "User can view other users' profiles"
    - "Avatar displays correctly after upload"
  artifacts:
    - path: "frontend/src/pages/ProfilePage.tsx"
      provides: "Own profile view and edit"
      contains: "display name input, avatar upload"
    - path: "frontend/src/components/AvatarUpload.tsx"
      provides: "Avatar upload with preview"
      contains: "react-dropzone, file validation"
    - path: "frontend/src/components/ProfilePrompt.tsx"
      provides: "Inline profile setup prompt (created but not triggered until Phase 2)"
      contains: "profile complete check"
  key_links:
    - from: "ProfilePage"
      to: "userApi.updateProfile"
      via: "form submission"
      pattern: "updateProfile"
    - from: "AvatarUpload"
      to: "userApi.uploadAvatar"
      via: "file drop/selection"
      pattern: "uploadAvatar"
    - from: "ProfilePrompt"
      to: "user.isProfileComplete"
      via: "conditional render"
      pattern: "profileComplete"
---

<objective>
Create profile page with display name editing and avatar upload. Create public profile view for other users. Create ProfilePrompt component for future profile completion enforcement (will be wired in Phase 2 when listings/chats exist).

Purpose: Complete the user profile management UI.
Output: Working profile pages with avatar upload. ProfilePrompt component created for Phase 2 integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-user-management/01-CONTEXT.md
@.planning/phases/01-foundation-and-user-management/01-UI-SPEC.md
@.planning/phases/01-foundation-and-user-management/01-RESEARCH.md

<interfaces>
<!-- Backend API from Plan 03 -->

**User API:**
- GET /api/users/me - returns: {id, displayName, avatarUrl, createdAt, listingCount, isOwnProfile}
- PUT /api/users/me - body: {displayName}, returns: UserProfileResponse
- GET /api/users/{id} - returns: {id, displayName, avatarUrl, createdAt, listingCount, isOwnProfile}
- POST /api/users/me/avatar - multipart/form-data with file, returns: {avatarUrl}

**Frontend components from Plan 04:**
- authStore: {user, isAuthenticated, setUser}
- apiClient: Axios instance with auth interceptors
- ProtectedRoute: Redirects unauthenticated users

**Locked Decisions (from CONTEXT.md):**
- D-06: Profile (display name) required before creating listings or starting chats
- D-07: Inline profile setup prompt when action requires profile
- D-08: Users without display name show "New User"
- D-10: Max avatar file size: 5 MB
- D-11: Supported formats: JPEG, PNG, WebP

**UI Contract (from UI-SPEC.md):**
- Profile Page: Avatar with edit overlay, display name (editable), bio textarea (optional), location (optional), "Save profile" button
- Avatar: Initial-based default with random background color when no image
- Form validation messages from UI-SPEC.md
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create profile page with display name editing</name>
  <files>frontend/src/pages/ProfilePage.tsx, frontend/src/api/userApi.ts, frontend/src/hooks/useUserProfile.ts</files>
  <read_first>
  - frontend/src/stores/authStore.ts (created in Plan 04)
  - frontend/src/api/client.ts (created in Plan 04)
  - .planning/phases/01-foundation-and-user-management/01-UI-SPEC.md (Profile Page, Default Avatar)
  - .planning/phases/01-foundation-and-user-management/01-CONTEXT.md (D-08)
  </read_first>
  <action>
  Create profile page with display name editing:

  **src/api/userApi.ts:**
  - getProfile(): Promise&lt;User&gt; - GET /api/users/me
  - updateProfile(data: {displayName: string | null}): Promise&lt;User&gt; - PUT /api/users/me
  - getUserById(id: number): Promise&lt;User&gt; - GET /api/users/{id}
  - uploadAvatar(file: File): Promise&lt;{avatarUrl: string}&gt; - POST /api/users/me/avatar (multipart)
  - deleteAvatar(): Promise&lt;void&gt; - DELETE /api/users/me/avatar

  **src/hooks/useUserProfile.ts:**
  - Custom hook for fetching and updating user profile
  - Use TanStack Query: useQuery for fetching, useMutation for updating
  - On successful update: Invalidate profile query, update authStore.user
  - Return: {profile, isLoading, error, updateProfile, isUpdating}

  **src/pages/ProfilePage.tsx:**
  - Use useUserProfile hook
  - Layout (from UI-SPEC.md): Card container with:
    - Avatar section (placeholder for AvatarUpload component in Task 2)
    - Display name input (editable)
    - Join date (read-only, formatted)
    - Listing count (read-only, placeholder 0 for now)
    - "Save profile" button (primary CTA)
  - Display name validation:
    - Max 100 characters
    - Error message: "Display name must be 50 characters or less" (UI-SPEC.md says 50, but backend allows 100 - use 50 for stricter frontend)
  - Show "New User" when displayName is null (D-08)
  - On save: Call updateProfile, show success toast
  - Use shadcn Card, Input, Label, Button components
  - Skeleton loading state while fetching
  - Responsive layout: centered card, max-width 600px

  **Default avatar implementation:**
  - When user has no avatarUrl, generate initials avatar:
    - Use first letter of displayName or first letter of email
    - Background color from curated palette (blue, green, purple, orange, pink, teal)
    - Hash user ID or email to select consistent color
  - UI-SPEC.md specifies colors: #3b82f6 (blue), #22c55e (green), #a855f7 (purple), #f97316 (orange), #ec4899 (pink), #14b8a6 (teal)
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | grep -E "(built|error)" && echo "Build successful"</automated>
  </verify>
  <done>
  - ProfilePage fetches and displays user profile
  - Display name can be edited and saved
  - Default avatar shows initials when no image uploaded (D-08)
  - Join date displayed in readable format
  - Save button calls updateProfile API
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create avatar upload component</name>
  <files>frontend/src/components/AvatarUpload.tsx, frontend/src/pages/ProfilePage.tsx</files>
  <read_first>
  - frontend/src/api/userApi.ts (created in Task 1)
  - .planning/phases/01-foundation-and-user-management/01-UI-SPEC.md (Avatar section)
  - .planning/phases/01-foundation-and-user-management/01-CONTEXT.md (D-10, D-11)
  </read_first>
  <action>
  Create avatar upload component with react-dropzone:

  **src/components/AvatarUpload.tsx:**
  - Props: currentAvatarUrl (string | null), onUploadComplete (url: string) => void, userName (string) for default avatar
  - Use react-dropzone for drag-and-drop and file picker
  - File validation:
    - Accept: image/jpeg, image/png, image/webp (D-11)
    - Max size: 5 MB (D-10)
    - Show error toast for invalid files
  - Features:
    - Click or drag-and-drop to upload
    - Show preview of current avatar (or default initials avatar)
    - Edit overlay on hover (pencil icon or "Change" text)
    - Loading spinner during upload
    - Success/error feedback via toast
  - On successful upload:
    - Call onUploadComplete with new URL
    - Update authStore.user.avatarUrl
  - Styling:
    - Circular avatar container (use shadcn Avatar component)
    - Size: 120x120 pixels for profile page
    - Border and shadow for visual polish
    - Hover state shows upload affordance
  - Initials avatar generation (same as Task 1 for consistency):
    - First letter of displayName or email
    - Consistent background color based on user hash

  **Update src/pages/ProfilePage.tsx:**
  - Import and use AvatarUpload component
  - Pass currentAvatarUrl from profile data
  - Handle onUploadComplete to refresh profile
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | grep -E "(built|error)" && echo "Build successful"</automated>
  </verify>
  <done>
  - AvatarUpload supports click and drag-and-drop
  - Validates file type (JPEG, PNG, WebP only) and size (max 5 MB)
  - Shows preview of current or default avatar
  - Uploads to backend and updates display
  - Shows loading state during upload
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create user profile view and profile prompt</name>
  <files>frontend/src/pages/UserProfilePage.tsx, frontend/src/components/ProfilePrompt.tsx, frontend/src/App.tsx</files>
  <read_first>
  - frontend/src/api/userApi.ts (created in Task 1)
  - .planning/phases/01-foundation-and-user-management/01-CONTEXT.md (D-06, D-07)
  - .planning/phases/01-foundation-and-user-management/01-UI-SPEC.md (Empty state)
  </read_first>
  <action>
  Create public profile view and profile completion prompt:

  **src/pages/UserProfilePage.tsx:**
  - Route: /users/:id
  - Fetch user by ID using useQuery and userApi.getUserById
  - Layout (similar to ProfilePage but read-only):
    - Avatar (initials default if no image)
    - Display name (or "New User" if null)
    - Join date
    - Listing count
  - No edit capabilities (public view)
  - Back navigation or link to return
  - Handle 404 (user not found) with error message
  - Loading skeleton while fetching
  - This satisfies PROF-04

  **src/components/ProfilePrompt.tsx:**
  - **IMPORTANT: This component is created in Phase 1 but will NOT be triggered until Phase 2.**
  - D-06 and D-07 apply to creating listings and starting chats, which don't exist in Phase 1.
  - The component exists for Phase 2 integration when those features are built.
  - Modal or inline form that prompts user to complete profile
  - Triggered when user attempts action requiring profile (D-06, D-07)
  - Props: isOpen (boolean), onComplete () => void, onClose () => void
  - Content (from UI-SPEC.md):
    - Heading: "Set up your profile"
    - Body: "Add a profile picture and bio to help others know who you are."
    - Display name input (required)
    - Optional: link to full profile page
    - "Save" button
  - On save:
    - Call updateProfile with display name
    - Set profileComplete to true
    - Call onComplete callback
    - Close modal
  - Use shadcn Dialog component for modal
  - Alternative: Inline form that appears in place of blocked content

  **Update src/App.tsx:**
  - Add route for /users/:id -> UserProfilePage
  - Ensure navigation links to user profiles work
  - Profile link in nav shows user's own profile
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | grep -E "(built|error)" && echo "Build successful"</automated>
  </verify>
  <done>
  - UserProfilePage displays public profile at /users/:id (PROF-04)
  - ProfilePrompt component created for Phase 2 integration (D-06, D-07)
  - Navigation supports viewing other users' profiles
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `cd frontend && npm run build` - must succeed
2. Navigate to /profile - should show own profile
3. Navigate to /users/1 - should show user profile (or 404)
4. Test avatar upload with valid image
5. Verify ProfilePrompt component exists (note: not triggered until Phase 2)
</verification>

<success_criteria>
- Profile page shows user data and allows display name editing (PROF-01)
- Avatar upload works with file validation (PROF-02, D-10, D-11)
- Profile shows join date and listing count (PROF-03)
- Public profile view shows other users (PROF-04)
- Default avatar shows "New User" for missing display name (D-08)
- ProfilePrompt component created for Phase 2 integration (D-06, D-07)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-user-management/01-05-SUMMARY.md`
</output>