---
phase: 01-foundation-and-user-management
plan: 06
type: execute
wave: 5
depends_on: [01-01-PLAN.md, 01-02-PLAN.md, 01-03-PLAN.md, 01-04-PLAN.md, 01-05-PLAN.md]
files_modified: []
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, PROF-01, PROF-02, PROF-03, PROF-04]
user_setup: []

must_haves:
  truths:
    - "Complete user registration flow works end-to-end"
    - "User can login and maintain session across refresh"
    - "User can upload avatar and see it on profile"
    - "User can view other users' profiles"
    - "Session persists across browser refresh (AUTH-03)"
  artifacts:
    - path: "backend/target/classes"
      provides: "Running backend application"
    - path: "frontend/dist"
      provides: "Built frontend application"
  key_links:
    - from: "Frontend login"
      to: "Backend /api/auth/login"
      via: "HTTP request"
      pattern: "fetch/axios"
    - from: "Browser localStorage"
      to: "Auth session"
      via: "token persistence"
      pattern: "localStorage.getItem"
---

<objective>
Verify the complete authentication and profile management flow works end-to-end. This is a human verification checkpoint to ensure all components integrate correctly before marking the phase complete.

Purpose: Confirm all Phase 1 requirements are met.
Output: Verified working authentication and profile system.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-user-management/01-CONTEXT.md
@.planning/phases/01-foundation-and-user-management/01-VALIDATION.md

<interfaces>
**Prerequisites (all must be complete before this checkpoint):**
- Plan 01: Backend project, User entity, test infrastructure
- Plan 02: Spring Security, JWT auth, auth endpoints
- Plan 03: Profile endpoints, avatar upload
- Plan 04: Frontend setup, auth UI
- Plan 05: Profile UI, avatar upload UI

**All Phase 1 Requirements:**
- AUTH-01: User can register with email and password
- AUTH-02: User can log in with email and password
- AUTH-03: User session persists across browser refresh (JWT-based)
- AUTH-04: User can log out from any page
- PROF-01: User can create profile with display name
- PROF-02: User can upload avatar image
- PROF-03: User can view their own profile with listing count and join date
- PROF-04: User can view other users' profiles

**Locked Decisions to Verify:**
- D-01: Immediate access after registration
- D-02: Registration collects only email/password
- D-03: Password minimum 8 characters
- D-04: Generic duplicate email error
- D-08: "New User" placeholder for missing display name
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete authentication and profile management system:
- Backend: Spring Boot API with JWT authentication, user profiles, avatar upload
- Frontend: React application with login/register, profile page, avatar upload
- Database: MySQL with users table
- Session: JWT tokens with refresh token rotation
  </what-built>
  <how-to-verify>
**Step 1: Start the application**
```bash
# Terminal 1: Start MySQL and Redis (Docker)
docker run -d --name mysql -e MYSQL_ROOT_PASSWORD=root -e MYSQL_DATABASE=trading_platform -p 3306:3306 mysql:8
docker run -d --name redis -p 6379:6379 redis:7-alpine

# Terminal 2: Start backend
cd backend
mvn spring-boot:run

# Terminal 3: Start frontend
cd frontend
npm run dev
```

**Step 2: Verify Registration (AUTH-01, D-01, D-02)**
1. Open http://localhost:5173/register
2. Enter email: test@example.com
3. Enter password: password123 (8+ chars, D-03)
4. Click "Create account"
5. Expected: Redirected to home page, logged in immediately (D-01)
6. Verify no display name field on registration (D-02)

**Step 3: Verify Logout (AUTH-04)**
1. Click "Sign out" in navigation
2. Expected: Redirected to login page
3. Refresh page
4. Expected: Still on login page (not authenticated)

**Step 4: Verify Login (AUTH-02)**
1. Enter email: test@example.com
2. Enter password: password123
3. Click "Sign in"
4. Expected: Redirected to home page, logged in
5. Try wrong password
6. Expected: Error toast "Invalid email or password"

**Step 5: Verify Session Persistence (AUTH-03)**
1. With user logged in, refresh the page (F5)
2. Expected: User remains logged in
3. Close browser tab, reopen http://localhost:5173
4. Expected: User remains logged in (refresh token persisted)

**Step 6: Verify Profile Display Name (PROF-01, D-08)**
1. Navigate to /profile
2. Expected: Display name shows "New User" (D-08)
3. Enter display name: "Test User"
4. Click "Save profile"
5. Expected: Success toast, name updated

**Step 7: Verify Avatar Upload (PROF-02)**
1. On profile page, click on avatar area
2. Select a JPEG or PNG image under 5 MB
3. Expected: Image uploads, shows in avatar circle
4. Refresh page
5. Expected: Avatar persists

**Step 8: Verify Public Profile (PROF-03, PROF-04)**
1. Note your user ID from URL or profile
2. Navigate to /users/{your-id}
3. Expected: Shows your profile (read-only)
4. Verify display name, avatar, join date visible

**Step 9: Verify Duplicate Email Error (D-04)**
1. Log out
2. Go to /register
3. Enter same email: test@example.com
4. Enter any password
5. Click "Create account"
6. Expected: Generic error "Email already registered" (D-04)
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
After human verification:
1. All AUTH-01 to AUTH-04 requirements confirmed working
2. All PROF-01 to PROF-04 requirements confirmed working
3. All locked decisions (D-01 to D-12) verified in implementation
4. No critical bugs found
</verification>

<success_criteria>
- User can register and login successfully
- Session persists across browser refresh
- User can set display name and upload avatar
- User can view own and other users' profiles
- All form validation works correctly
- Error messages display appropriately
- Avatar uploads work with file validation
</success_criteria>

<output>
After approval, create `.planning/phases/01-foundation-and-user-management/01-06-SUMMARY.md`

Then update ROADMAP.md to mark Phase 1 complete and proceed to Phase 2 planning.
</output>