---
phase: 02-core-marketplace-listings-and-discovery
plan: 01
type: execute
wave: 1
depends_on: [02-00]
files_modified:
  - backend/src/main/resources/db/changelog/002-create-categories-table.xml
  - backend/src/main/resources/db/changelog/003-create-listings-table.xml
  - backend/src/main/resources/db/changelog/004-create-listing-images-table.xml
  - backend/src/main/resources/db/changelog/db.changelog-master.xml
  - backend/src/main/java/com/tradingplatform/listing/Category.java
  - backend/src/main/java/com/tradingplatform/listing/Condition.java
  - backend/src/main/java/com/tradingplatform/listing/ListingStatus.java
  - backend/src/test/java/com/tradingplatform/listing/repository/CategoryRepositoryTest.java
  - backend/src/test/java/com/tradingplatform/listing/repository/ListingRepositoryTest.java
autonomous: true
requirements: [LIST-03, LIST-04, LIST-05, LIST-08]
user_setup: []

must_haves:
  truths:
    - "Categories table exists with parent_id for hierarchy"
    - "Listings table exists with all required columns and indexes"
    - "Listing images table exists with primary flag and display order"
    - "FULLTEXT index exists on listings(title, description)"
    - "Enum types are defined: Condition, ListingStatus"
  artifacts:
    - path: "backend/src/main/resources/db/changelog/002-create-categories-table.xml"
      provides: "Category schema with hierarchy"
      contains: "parent_id"
    - path: "backend/src/main/resources/db/changelog/003-create-listings-table.xml"
      provides: "Listing schema with foreign keys"
      contains: "user_id, category_id, price, condition, status"
    - path: "backend/src/main/resources/db/changelog/004-create-listing-images-table.xml"
      provides: "Image schema with primary flag"
      contains: "is_primary, display_order"
    - path: "backend/src/main/java/com/tradingplatform/listing/Category.java"
      provides: "Category entity with parent reference"
      exports: ["Category"]
    - path: "backend/src/main/java/com/tradingplatform/listing/Condition.java"
      provides: "Item condition enum"
      contains: "NEW, LIKE_NEW, GOOD, FAIR, POOR"
    - path: "backend/src/main/java/com/tradingplatform/listing/ListingStatus.java"
      provides: "Listing status enum"
      contains: "AVAILABLE, RESERVED, SOLD"
  key_links:
    - from: "categories.parent_id"
      to: "categories.id"
      via: "self-referencing FK"
      pattern: "fk_categories_parent"
    - from: "listings.user_id"
      to: "users.id"
      via: "foreign key"
      pattern: "fk_listings_user"
    - from: "listings.category_id"
      to: "categories.id"
      via: "foreign key"
      pattern: "fk_listings_category"
---

<objective>
Create database schema for categories, listings, and listing images. Define enum types for condition and status. Implement Category entity with repository test implementations.

Purpose: Foundation for all listing-related data storage with proper indexes for search performance.
Output: Liquibase migrations, enum classes, Category entity, passing repository tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-marketplace-listings-and-discovery/02-RESEARCH.md
@.planning/phases/02-core-marketplace-listings-and-discovery/02-00-PLAN.md

<interfaces>
<!-- Existing patterns from Phase 1 codebase -->

From backend/src/main/resources/db/changelog/001-create-users-table.xml:
```xml
<changeSet id="001-create-users-table" author="system">
    <createTable tableName="users">
        <column name="id" type="BIGINT" autoIncrement="true">
            <constraints primaryKey="true" nullable="false"/>
        </column>
        <!-- ... -->
    </createTable>
    <createIndex tableName="users" indexName="idx_users_email" unique="true">
        <column name="email"/>
    </createIndex>
</changeSet>
```

From backend/src/main/resources/db/changelog/db.changelog-master.xml:
```xml
<include file="classpath:db/changelog/001-create-users-table.xml"/>
```

From backend/src/main/java/com/tradingplatform/user/User.java:
```java
@Entity
@Table(name = "users", indexes = {
    @Index(name = "idx_users_email", columnList = "email", unique = true)
})
@EntityListeners(AuditingEntityListener.class)
@Getter @Setter @NoArgsConstructor @AllArgsConstructor @Builder
public class User {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    // ...
}
```

From backend/src/main/java/com/tradingplatform/exception/ErrorCode.java:
```java
@Getter
public enum ErrorCode {
    USER_NOT_FOUND(HttpStatus.NOT_FOUND, "User not found"),
    // ...
    private final HttpStatus httpStatus;
    private final String message;
    ErrorCode(HttpStatus httpStatus, String message) { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Category entity and Liquibase migration</name>
  <files>
    backend/src/main/java/com/tradingplatform/listing/Category.java,
    backend/src/main/resources/db/changelog/002-create-categories-table.xml,
    backend/src/main/resources/db/changelog/db.changelog-master.xml,
    backend/src/test/java/com/tradingplatform/listing/repository/CategoryRepositoryTest.java
  </files>
  <read_first>
    backend/src/main/java/com/tradingplatform/user/User.java,
    backend/src/main/resources/db/changelog/001-create-users-table.xml,
    backend/src/main/resources/db/changelog/db.changelog-master.xml
  </read_first>
  <behavior>
    - Test 1: CategoryRepository finds by parent_id returning empty list for null parent
    - Test 2: CategoryRepository finds by slug returning Optional
    - Test 3: Category entity has isLeaf() and getDepth() methods working
  </behavior>
  <action>
    1. Create `backend/src/main/java/com/tradingplatform/listing/Category.java` entity:
       - Package: `com.tradingplatform.listing`
       - Fields: `id` (Long, generated), `name` (String, 100 chars, not null), `slug` (String, 100 chars, unique, not null), `description` (String, 500 chars), `parent` (ManyToOne to Category), `children` (OneToMany(mappedBy="parent")), `displayOrder` (Integer, default 0)
       - Add `@OrderBy("name ASC")` on children
       - Add `@Transient` methods: `isLeaf()` returns children.isEmpty(), `getDepth()` counts parent chain
       - Use same annotation pattern as User.java: `@Entity`, `@Table`, `@EntityListeners(AuditingEntityListener.class)`, `@Getter`, `@Setter`, `@NoArgsConstructor`, `@AllArgsConstructor`, `@Builder`

    2. Create `backend/src/main/resources/db/changelog/002-create-categories-table.xml`:
       - Follow exact XML structure from 001-create-users-table.xml
       - Table: `categories` with columns: id (BIGINT PK auto), name (VARCHAR(100) NOT NULL), slug (VARCHAR(100) NOT NULL UNIQUE), description (VARCHAR(500)), parent_id (BIGINT), display_order (INT DEFAULT 0)
       - Create index `idx_categories_parent` on parent_id
       - Create unique index `idx_categories_slug` on slug
       - Add FK constraint `fk_categories_parent` referencing categories(id)

    3. Update `backend/src/main/resources/db/changelog/db.changelog-master.xml`:
       - Add `<include file="classpath:db/changelog/002-create-categories-table.xml"/>` after the 001 include

    4. Implement tests in `backend/src/test/java/com/tradingplatform/listing/repository/CategoryRepositoryTest.java`:
       - Remove `// TODO: Implement` and add actual test implementations
       - Use Testcontainers MySQL for integration tests
       - Test findByParentId with null and non-null values
       - Test findBySlug with existing and non-existing slugs
       - Test category hierarchy with parent-child relationships
  </action>
  <verify>
    <automated>mvn test -Dtest=CategoryRepositoryTest -q</automated>
  </verify>
  <done>
    - Category.java exists with all fields and @Transient methods
    - 002-create-categories-table.xml has parent_id FK to self
    - db.changelog-master.xml includes new migration file
    - CategoryRepositoryTest passes with 3 implemented tests
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Listing enums and Liquibase migration</name>
  <files>
    backend/src/main/java/com/tradingplatform/listing/Condition.java,
    backend/src/main/java/com/tradingplatform/listing/ListingStatus.java,
    backend/src/main/resources/db/changelog/003-create-listings-table.xml,
    backend/src/main/resources/db/changelog/db.changelog-master.xml
  </files>
  <read_first>
    backend/src/main/java/com/tradingplatform/exception/ErrorCode.java,
    backend/src/main/resources/db/changelog/001-create-users-table.xml
  </read_first>
  <behavior>
    - Test 1: Condition enum contains exactly 5 values: NEW, LIKE_NEW, GOOD, FAIR, POOR
    - Test 2: ListingStatus enum contains exactly 3 values: AVAILABLE, RESERVED, SOLD
    - Test 3: Listings table has foreign key to users and categories
    - Test 4: Listings table has FULLTEXT index on title and description
  </behavior>
  <action>
    1. Create `backend/src/main/java/com/tradingplatform/listing/Condition.java` enum:
       - Package: `com.tradingplatform.listing`
       - Values: `NEW`, `LIKE_NEW`, `GOOD`, `FAIR`, `POOR`
       - Simple enum, no additional fields (use same pattern as ErrorCode but without httpStatus/message)

    2. Create `backend/src/main/java/com/tradingplatform/listing/ListingStatus.java` enum:
       - Package: `com.tradingplatform.listing`
       - Values: `AVAILABLE`, `RESERVED`, `SOLD`
       - Simple enum

    3. Create `backend/src/main/resources/db/changelog/003-create-listings-table.xml`:
       - Follow exact XML structure from 001-create-users-table.xml
       - Table: `listings` with columns:
         - id (BIGINT PK auto)
         - title (VARCHAR(200) NOT NULL)
         - description (TEXT NOT NULL)
         - price (DECIMAL(10,2) NOT NULL)
         - condition (VARCHAR(20) NOT NULL)
         - status (VARCHAR(20) NOT NULL)
         - user_id (BIGINT NOT NULL)
         - category_id (BIGINT NOT NULL)
         - city (VARCHAR(100))
         - region (VARCHAR(100))
         - latitude (DOUBLE)
         - longitude (DOUBLE)
         - created_at (TIMESTAMP NOT NULL)
         - updated_at (TIMESTAMP)
         - is_deleted (BOOLEAN DEFAULT FALSE NOT NULL)
       - Create indexes:
         - idx_listings_user on user_id
         - idx_listings_category on category_id
         - idx_listings_status on status
         - idx_listings_price on price
       - Add FK constraints:
         - fk_listings_user referencing users(id)
         - fk_listings_category referencing categories(id)
       - Add FULLTEXT index in separate changeSet:
         - `<sql dbms="mysql">CREATE FULLTEXT INDEX idx_listings_fulltext ON listings(title, description)</sql>`

    4. Update `backend/src/main/resources/db/changelog/db.changelog-master.xml`:
       - Add `<include file="classpath:db/changelog/003-create-listings-table.xml"/>`
  </action>
  <verify>
    <automated>mvn compile -q && grep -q "enum Condition" backend/src/main/java/com/tradingplatform/listing/Condition.java && grep -q "FULLTEXT" backend/src/main/resources/db/changelog/003-create-listings-table.xml</automated>
  </verify>
  <done>
    - Condition.java has 5 enum values
    - ListingStatus.java has 3 enum values
    - 003-create-listings-table.xml has all columns and FULLTEXT index
    - Foreign keys to users and categories defined
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create ListingImage migration and implement ListingRepository tests</name>
  <files>
    backend/src/main/resources/db/changelog/004-create-listing-images-table.xml,
    backend/src/main/resources/db/changelog/db.changelog-master.xml,
    backend/src/test/java/com/tradingplatform/listing/repository/ListingRepositoryTest.java
  </files>
  <read_first>
    backend/src/main/resources/db/changelog/001-create-users-table.xml,
    backend/src/main/resources/db/changelog/003-create-listings-table.xml
  </read_first>
  <behavior>
    - Test 1: ListingImage table has is_primary column
    - Test 2: ListingImage has cascade delete on listing_id FK
    - Test 3: ListingRepositoryTest stubs are implemented and pass
  </behavior>
  <action>
    1. Create `backend/src/main/resources/db/changelog/004-create-listing-images-table.xml`:
       - Follow exact XML structure from existing migrations
       - Table: `listing_images` with columns:
         - id (BIGINT PK auto)
         - listing_id (BIGINT NOT NULL)
         - image_path (VARCHAR(500) NOT NULL)
         - is_primary (BOOLEAN DEFAULT FALSE NOT NULL)
         - display_order (INT DEFAULT 0 NOT NULL)
         - created_at (TIMESTAMP NOT NULL)
       - Create index: idx_images_listing on listing_id
       - Add FK constraint: fk_images_listing referencing listings(id) with onDelete="CASCADE"

    2. Update `backend/src/main/resources/db/changelog/db.changelog-master.xml`:
       - Add `<include file="classpath:db/changelog/004-create-listing-images-table.xml"/>`

    3. Implement tests in `backend/src/test/java/com/tradingplatform/listing/repository/ListingRepositoryTest.java`:
       - Remove `// TODO: Implement` and add actual test implementations
       - Note: Full tests require Listing entity (created in Plan 02), so implement basic tests that verify:
         - Repository can be injected (DI works)
         - Basic save/find operations work
       - Leave full-text search and complex filter tests as stubs until Listing entity exists
  </action>
  <verify>
    <automated>mvn test -Dtest=ListingRepositoryTest -q</automated>
  </verify>
  <done>
    - 004-create-listing-images-table.xml has is_primary and cascade delete
    - db.changelog-master.xml includes all 4 migration files
    - ListingRepositoryTest compiles and runs basic tests
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `mvn test -Dtest=CategoryRepositoryTest,ListingRepositoryTest -q` - must succeed
2. Verify migration files are in correct order in db.changelog-master.xml
3. Verify all enum values match RESEARCH.md specifications
</verification>

<success_criteria>
- All 4 Liquibase migration files exist and are included in master changelog
- Category entity has self-referencing parent relationship
- Condition enum has NEW, LIKE_NEW, GOOD, FAIR, POOR
- ListingStatus enum has AVAILABLE, RESERVED, SOLD
- FULLTEXT index defined on listings(title, description)
- CategoryRepositoryTest passes with implemented tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-marketplace-listings-and-discovery/02-01-SUMMARY.md`
</output>