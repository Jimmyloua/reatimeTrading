---
phase: 08-public-discovery-access-and-profile-surface-integration-repair
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - backend/src/main/java/com/tradingplatform/user/UserController.java
  - backend/src/main/java/com/tradingplatform/listing/repository/ListingRepository.java
  - backend/src/test/java/com/tradingplatform/controller/UserControllerTest.java
  - frontend/src/pages/UserProfilePage.tsx
  - frontend/src/tests/user-profile-page.test.tsx
autonomous: true
requirements:
  - PROF-03
  - PROF-04
must_haves:
  truths:
    - "The signed-in profile and the public profile both show listing counts derived from real listings instead of a hardcoded zero."
    - "Anonymous users can open a public profile and see the rating section without being redirected away from `/users/{id}`."
    - "The public profile header never renders `@undefined` when email is omitted from the API response."
  artifacts:
    - path: "backend/src/main/java/com/tradingplatform/user/UserController.java"
      provides: "Listing-backed profile response generation for /api/users/me and /api/users/{id}"
    - path: "backend/src/main/java/com/tradingplatform/listing/repository/ListingRepository.java"
      provides: "Count query for non-deleted listings per seller"
    - path: "backend/src/test/java/com/tradingplatform/controller/UserControllerTest.java"
      provides: "Automated proof that profile counts change with listing creation or deletion"
    - path: "frontend/src/pages/UserProfilePage.tsx"
      provides: "Anonymous-safe public profile handle and rating section rendering"
    - path: "frontend/src/tests/user-profile-page.test.tsx"
      provides: "Public profile regression test for anonymous viewing"
  key_links:
    - from: "backend/src/main/java/com/tradingplatform/user/UserController.java"
      to: "backend/src/main/java/com/tradingplatform/listing/repository/ListingRepository.java"
      via: "listing-count aggregation"
      pattern: "countByUserIdAndDeletedFalse"
    - from: "frontend/src/pages/UserProfilePage.tsx"
      to: "frontend/src/hooks/useUserProfile.ts"
      via: "public profile query"
      pattern: "usePublicProfile"
    - from: "frontend/src/pages/UserProfilePage.tsx"
      to: "frontend/src/components/profile/ProfileRatingSection.tsx"
      via: "public rating surface"
      pattern: "ProfileRatingSection"
---

<objective>
Replace the remaining hardcoded profile listing count with live listing-backed data and repair the public profile page so anonymous users see stable identity and reputation surfaces.

Purpose: Phase 01 left `listingCount` as a stub, and the public profile UI still derives a handle from an email field that is intentionally omitted for anonymous viewers. This plan makes profile data truthful and fixes the public profile presentation bug without widening scope beyond the shipped profile surface.
Output: Listing-backed profile counts for `/api/users/me` and `/api/users/{id}`, plus a public profile regression test that proves anonymous viewing works end to end.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-RESEARCH.md
@.planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-VALIDATION.md
@backend/src/main/java/com/tradingplatform/user/UserController.java
@backend/src/main/java/com/tradingplatform/listing/repository/ListingRepository.java
@backend/src/test/java/com/tradingplatform/controller/UserControllerTest.java
@frontend/src/pages/UserProfilePage.tsx
@frontend/src/components/profile/ProfileRatingSection.tsx
@frontend/src/hooks/useUserProfile.ts
@frontend/src/api/userApi.ts
@frontend/src/api/ratingApi.ts
@.planning/phases/01-foundation-and-user-management/01-03-SUMMARY.md
@.planning/phases/04-transactions-and-trust/04-07-SUMMARY.md

<interfaces>
Current profile DTO contract:
```java
public class UserProfileResponse {
    private Long id;
    private String email;
    private String displayName;
    private String avatarUrl;
    private LocalDateTime createdAt;
    private Long listingCount;
    private boolean ownProfile;
}
```

Public profile route:
```text
GET /api/users/{id}
Route path: /users/:id
```

Rating panel queries used by the public profile:
```text
GET /api/ratings/users/{userId}/recent
GET /api/ratings/users/{userId}/summary
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace hardcoded profile listing counts with listing-backed aggregation</name>
  <read_first>
    - backend/src/main/java/com/tradingplatform/user/UserController.java
    - backend/src/main/java/com/tradingplatform/listing/repository/ListingRepository.java
    - backend/src/test/java/com/tradingplatform/controller/UserControllerTest.java
    - backend/src/main/java/com/tradingplatform/listing/service/ListingService.java
    - .planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-RESEARCH.md
  </read_first>
  <files>backend/src/main/java/com/tradingplatform/user/UserController.java, backend/src/main/java/com/tradingplatform/listing/repository/ListingRepository.java, backend/src/test/java/com/tradingplatform/controller/UserControllerTest.java</files>
  <acceptance_criteria>
    - `ListingRepository.java` contains a `countByUserIdAndDeletedFalse` query method.
    - `UserController.java` no longer contains `listingCount(0L)`.
    - `UserControllerTest.java` creates at least one listing and asserts non-zero `$.listingCount` for both `/api/users/me` and `/api/users/{id}`.
    - `UserControllerTest.java` contains a delete or soft-delete assertion proving removed listings stop counting.
    - `cd backend && mvn -q -Dtest=UserControllerTest test` passes.
  </acceptance_criteria>
  <action>Add a repository-level aggregation for seller listing counts based on non-deleted listings, then use that value when building `UserProfileResponse` for both `GET /api/users/me` and `GET /api/users/{id}`. Keep the response shape unchanged. Extend `UserControllerTest` with real listing setup so the test proves counts change after listing creation and after deletion or soft-delete, rather than just checking the old zero stub.</action>
  <verify>
    <automated>cd backend && mvn -q -Dtest=UserControllerTest test</automated>
  </verify>
  <done>Profile responses return truthful listing totals derived from actual listing rows, and the contract is pinned by backend integration coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair the anonymous public profile surface and lock it with a frontend regression test</name>
  <read_first>
    - frontend/src/pages/UserProfilePage.tsx
    - frontend/src/components/profile/ProfileRatingSection.tsx
    - frontend/src/hooks/useUserProfile.ts
    - frontend/src/api/client.ts
    - frontend/src/App.tsx
    - .planning/phases/04-transactions-and-trust/04-07-SUMMARY.md
  </read_first>
  <files>frontend/src/pages/UserProfilePage.tsx, frontend/src/tests/user-profile-page.test.tsx</files>
  <acceptance_criteria>
    - `frontend/src/pages/UserProfilePage.tsx` does not contain `profile.email?.split('@')[0]`.
    - `frontend/src/pages/UserProfilePage.tsx` renders a deterministic public handle fallback that uses profile data available to anonymous viewers.
    - `frontend/src/pages/UserProfilePage.tsx` still renders `ProfileRatingSection userId={profile.id}`.
    - `frontend/src/tests/user-profile-page.test.tsx` exists and renders `/users/:id` without a login redirect dependency.
    - `frontend/src/tests/user-profile-page.test.tsx` asserts the listing count, public handle, and ratings section are visible for an anonymous viewer.
    - `cd frontend && npm test -- --run src/tests/user-profile-page.test.tsx` passes.
  </acceptance_criteria>
  <action>Update `UserProfilePage.tsx` so the secondary public identity line never depends on `email`, because public profiles intentionally omit it. Use a deterministic anonymous-safe fallback such as a sanitized display-name slug with `user-{id}` fallback, and keep the current hero/card layout intact. Add `src/tests/user-profile-page.test.tsx` that renders the public `/users/:id` route for a logged-out viewer, mocks the profile and rating queries, and proves the page shows the truthful listing count plus the ratings section without routing to `/login` or rendering `@undefined`.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/tests/user-profile-page.test.tsx</automated>
  </verify>
  <done>The anonymous public profile route is stable, truthful, and no longer exposes the `@undefined` handle bug.</done>
</task>

</tasks>

<verification>
Run the focused backend profile suite and the focused frontend public-profile suite. The profile route must remain public, truthful, and compatible with the existing ratings panel.
</verification>

<success_criteria>
Both self and public profiles show real listing totals from listing data, and the anonymous public profile page renders a stable identity + ratings surface without login-only assumptions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-02-SUMMARY.md`
</output>
