---
phase: 08-public-discovery-access-and-profile-surface-integration-repair
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - frontend/src/tests/homepage-modules.test.tsx
  - frontend/src/tests/browse-category-hover.test.tsx
  - .planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-VERIFICATION.md
autonomous: false
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - DISC-05
  - P7-01
  - P7-02
  - P7-03
  - PROF-04
must_haves:
  truths:
    - "Homepage module links still resolve to the exact public browse URLs after backend auth changes."
    - "Browse category disclosure still commits only explicit selections into shareable browse URLs."
    - "A logged-out browser can open home, browse, and public profile surfaces without backend authorization breakage."
  artifacts:
    - path: "frontend/src/tests/homepage-modules.test.tsx"
      provides: "Exact browse URL assertions for homepage entry points"
    - path: "frontend/src/tests/browse-category-hover.test.tsx"
      provides: "Exact browse URL assertions for category disclosure entry points"
    - path: ".planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-VERIFICATION.md"
      provides: "Phase 08 automation evidence and manual approval record"
  key_links:
    - from: "frontend/src/tests/homepage-modules.test.tsx"
      to: "frontend/src/pages/HomePage.tsx"
      via: "CTA href assertions"
      pattern: "/listings\\?categoryId=|/listings\\?collection="
    - from: "frontend/src/tests/browse-category-hover.test.tsx"
      to: "frontend/src/pages/BrowseListingsPage.tsx"
      via: "search-param assertions"
      pattern: "categoryId=|collection="
    - from: ".planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-VERIFICATION.md"
      to: "backend/frontend targeted suites"
      via: "documented gate commands"
      pattern: "mvn -q|npm test -- --run"
---

<objective>
Close Phase 08 with a dedicated verification gate that proves the repaired anonymous discovery and public profile flows still match the Phase 07 browse-entry contracts.

Purpose: The backend fixes in 08-01 and the profile/data fixes in 08-02 are only complete if the homepage modules, browse disclosure, and public profile routes continue to resolve the same shareable browse URLs and work for a logged-out user. This plan turns that into explicit automation plus one blocking human verification pass.
Output: Refreshed frontend browse-entry regression tests, a Phase 08 verification artifact, and recorded human approval for the logged-out discovery/profile flow.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-RESEARCH.md
@.planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-VALIDATION.md
@frontend/src/tests/homepage-modules.test.tsx
@frontend/src/tests/browse-category-hover.test.tsx
@frontend/src/tests/user-profile-page.test.tsx
@.planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-03-SUMMARY.md
@.planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-05-SUMMARY.md

<interfaces>
Browse URL contracts that must remain true:
```text
/listings?categoryId={id}
/listings?collection={slug}
```

Manual surfaces to verify while logged out:
```text
/
/listings
/users/{id}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh the anonymous discovery regression gate and record the exact verification commands</name>
  <read_first>
    - frontend/src/tests/homepage-modules.test.tsx
    - frontend/src/tests/browse-category-hover.test.tsx
    - frontend/src/tests/user-profile-page.test.tsx
    - .planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-VALIDATION.md
    - .planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-03-SUMMARY.md
  </read_first>
  <files>frontend/src/tests/homepage-modules.test.tsx, frontend/src/tests/browse-category-hover.test.tsx, .planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-VERIFICATION.md</files>
  <acceptance_criteria>
    - `homepage-modules.test.tsx` contains explicit href assertions for both `/listings?categoryId=` and `/listings?collection=`.
    - `browse-category-hover.test.tsx` contains search-param assertions proving explicit selection commits the browse URL while preview-only hover does not.
    - `.planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-VERIFICATION.md` exists.
    - `08-VERIFICATION.md` contains the backend command `mvn -q -Dtest=UserControllerTest,ContentControllerTest,ListingControllerIT,RatingControllerTest test`.
    - `08-VERIFICATION.md` contains the frontend command `npm test -- --run src/tests/homepage-modules.test.tsx src/tests/browse-category-hover.test.tsx src/tests/user-profile-page.test.tsx`.
    - `cd backend && mvn -q -Dtest=UserControllerTest,ContentControllerTest,ListingControllerIT,RatingControllerTest test` passes.
    - `cd frontend && npm test -- --run src/tests/homepage-modules.test.tsx src/tests/browse-category-hover.test.tsx src/tests/user-profile-page.test.tsx` passes.
  </acceptance_criteria>
  <action>Keep the Phase 07 browse-entry assertions explicit and up to date after the anonymous-access repairs: homepage modules must still point to the exact public browse URLs, and the category disclosure must still preserve URL safety until the user explicitly selects a child category. Create `08-VERIFICATION.md` and record the exact backend/frontend commands, what each suite proves, and the manual checklist that closes the remaining logged-out flow validation. Do not weaken the assertions into generic smoke tests.</action>
  <verify>
    <automated>cd backend && mvn -q -Dtest=UserControllerTest,ContentControllerTest,ListingControllerIT,RatingControllerTest test && cd ../frontend && npm test -- --run src/tests/homepage-modules.test.tsx src/tests/browse-category-hover.test.tsx src/tests/user-profile-page.test.tsx</automated>
  </verify>
  <done>Phase 08 has a written verification artifact plus green automation that proves the homepage, browse disclosure, and public profile contracts together.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm the repaired anonymous discovery and public profile flow in a logged-out browser</name>
  <read_first>
    - .planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-VERIFICATION.md
    - frontend/src/tests/homepage-modules.test.tsx
    - frontend/src/tests/browse-category-hover.test.tsx
    - frontend/src/tests/user-profile-page.test.tsx
  </read_first>
  <files>.planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-VERIFICATION.md</files>
  <acceptance_criteria>
    - The user explicitly confirms the logged-out checks passed or provides concrete failures to address.
    - The manual checklist covers `/`, `/listings`, and `/users/{id}`.
    - The manual checklist verifies a homepage module CTA and a browse disclosure CTA resolve to the expected browse URLs.
  </acceptance_criteria>
  <action>Present the manual verification only after Task 1 automation is green, then record the user's approval or the reported failing steps in `08-VERIFICATION.md`. Do not mark the plan complete without an explicit user response because this checkpoint closes the logged-out integration gate for Phase 08.</action>
  <what-built>Anonymous discovery and public profile repairs with targeted automated regression coverage for homepage modules, browse disclosure, and public profile rendering.</what-built>
  <how-to-verify>
    1. Open the app in a logged-out browser session.
    2. Visit `/` and confirm homepage content loads without redirecting to `/login` or failing on module requests.
    3. Click a homepage module CTA and confirm the browser lands on `/listings?categoryId={id}` or `/listings?collection={slug}` as expected.
    4. Visit `/listings`, open the category disclosure, choose a child category, and confirm the URL updates to the selected `categoryId` only after the explicit action.
    5. Visit `/users/{id}` for a user with listings and confirm the page loads while logged out, the listing count is non-zero when expected, and the ratings section renders.
  </how-to-verify>
  <verify>
    <automated>Record the explicit human approval or failure notes in .planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-VERIFICATION.md after Task 1 automation passes.</automated>
  </verify>
  <done>The logged-out manual pass is either approved and recorded in `08-VERIFICATION.md`, or concrete failing steps are captured for follow-up work.</done>
  <resume-signal>Type `approved` if all checks pass, or reply with the failing step numbers and observed behavior.</resume-signal>
</task>

</tasks>

<verification>
Automation must be green before the manual checkpoint is presented. Record the command outputs and the human approval result in `08-VERIFICATION.md`.
</verification>

<success_criteria>
Phase 08 closes only when both automation and a logged-out manual pass confirm the repaired discovery/profile surfaces still route to the correct browse URLs and remain publicly accessible.
</success_criteria>

<output>
After completion, create `.planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-03-SUMMARY.md`
</output>
