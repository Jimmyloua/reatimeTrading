---
phase: 08-public-discovery-access-and-profile-surface-integration-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/tradingplatform/config/SecurityConfig.java
  - backend/src/test/java/com/tradingplatform/content/controller/ContentControllerTest.java
  - backend/src/test/java/com/tradingplatform/listing/controller/ListingControllerIT.java
  - backend/src/test/java/com/tradingplatform/transaction/controller/RatingControllerTest.java
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - DISC-05
  - P7-01
  - P7-02
  - P7-03
must_haves:
  truths:
    - "Anonymous users can load browse listings, category data, homepage modules, curated collections, and public rating reads without a 401."
    - "Anonymous access is opened only for read-only discovery endpoints; listing writes and rating submission remain authenticated."
    - "Homepage and browse entry links resolve against public backend APIs instead of authenticated-only routes."
  artifacts:
    - path: "backend/src/main/java/com/tradingplatform/config/SecurityConfig.java"
      provides: "Matcher-based anonymous GET authorization for discovery and rating read endpoints"
    - path: "backend/src/test/java/com/tradingplatform/content/controller/ContentControllerTest.java"
      provides: "MockMvc contract coverage for anonymous homepage and collection reads"
    - path: "backend/src/test/java/com/tradingplatform/listing/controller/ListingControllerIT.java"
      provides: "Integration coverage for anonymous listing browse and category reads"
    - path: "backend/src/test/java/com/tradingplatform/transaction/controller/RatingControllerTest.java"
      provides: "Anonymous public rating read coverage and protected write coverage"
  key_links:
    - from: "backend/src/main/java/com/tradingplatform/config/SecurityConfig.java"
      to: "backend/src/main/java/com/tradingplatform/content/controller/ContentController.java"
      via: "permitAll GET matchers for /api/content"
      pattern: "/api/content"
    - from: "backend/src/main/java/com/tradingplatform/config/SecurityConfig.java"
      to: "backend/src/main/java/com/tradingplatform/listing/controller/ListingController.java"
      via: "permitAll GET matchers for browse and category routes"
      pattern: "/api/listings"
    - from: "backend/src/main/java/com/tradingplatform/config/SecurityConfig.java"
      to: "backend/src/main/java/com/tradingplatform/transaction/controller/RatingController.java"
      via: "permitAll GET matchers for public rating reads only"
      pattern: "/api/ratings/users"
---

<objective>
Repair backend authorization so the anonymous discovery surfaces added in Phases 02, 04, and 07 can actually read their backing APIs without opening any write endpoints.

Purpose: The frontend already exposes public home, browse, and public profile routes. This plan aligns Spring Security with those shipped surfaces and replaces the current placeholder content test with real endpoint coverage.
Output: Updated discovery/rating security rules plus backend regression tests that prove anonymous reads succeed while writes stay protected.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-RESEARCH.md
@.planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-VALIDATION.md
@backend/src/main/java/com/tradingplatform/config/SecurityConfig.java
@backend/src/main/java/com/tradingplatform/listing/controller/ListingController.java
@backend/src/main/java/com/tradingplatform/content/controller/ContentController.java
@backend/src/main/java/com/tradingplatform/transaction/controller/RatingController.java
@backend/src/test/java/com/tradingplatform/content/controller/ContentControllerTest.java
@backend/src/test/java/com/tradingplatform/listing/controller/ListingControllerIT.java

<interfaces>
Read-only endpoints that must be anonymously accessible after this plan:
```text
GET /api/listings
GET /api/listings/categories
GET /api/listings/categories/{id}
GET /api/listings/{id}
GET /api/listings/user/{userId}
GET /api/content/homepage
GET /api/content/collections/{slug}
GET /api/ratings/users/{userId}
GET /api/ratings/users/{userId}/recent
GET /api/ratings/users/{userId}/summary
```

Endpoints that must remain authenticated:
```text
POST /api/listings
PUT /api/listings/{id}
PATCH /api/listings/{id}/status
POST /api/ratings/transactions/{transactionId}
GET /api/ratings/transactions/{transactionId}/can-rate
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace placeholder auth coverage with real anonymous discovery read tests</name>
  <read_first>
    - backend/src/test/java/com/tradingplatform/content/controller/ContentControllerTest.java
    - backend/src/test/java/com/tradingplatform/listing/controller/ListingControllerIT.java
    - backend/src/main/java/com/tradingplatform/content/controller/ContentController.java
    - backend/src/main/java/com/tradingplatform/listing/controller/ListingController.java
    - backend/src/main/java/com/tradingplatform/transaction/controller/RatingController.java
    - .planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-RESEARCH.md
    - .planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-VALIDATION.md
  </read_first>
  <files>backend/src/test/java/com/tradingplatform/content/controller/ContentControllerTest.java, backend/src/test/java/com/tradingplatform/listing/controller/ListingControllerIT.java, backend/src/test/java/com/tradingplatform/transaction/controller/RatingControllerTest.java</files>
  <acceptance_criteria>
    - `ContentControllerTest.java` uses `MockMvc` and contains `get(\"/api/content/homepage\")` plus `get(\"/api/content/collections/{slug}\"`.
    - `ListingControllerIT.java` contains anonymous `get(\"/api/listings\")`, `get(\"/api/listings/categories\")`, and `get(\"/api/listings/categories/{id}\"` coverage.
    - `RatingControllerTest.java` exists and contains anonymous reads for `/api/ratings/users/{userId}`, `/recent`, and `/summary`.
    - `RatingControllerTest.java` contains protected-write assertions for `post(\"/api/ratings/transactions/{transactionId}\"` and `get(\"/api/ratings/transactions/{transactionId}/can-rate\"`.
    - `cd backend && mvn -q -Dtest=ContentControllerTest,ListingControllerIT,RatingControllerTest test` passes.
  </acceptance_criteria>
  <action>Create real Spring Boot MockMvc coverage for the anonymous discovery contract before changing security. Replace the current source-string `ContentControllerTest` with request/response tests against `/api/content/homepage` and `/api/content/collections/{slug}`. Extend `ListingControllerIT` so anonymous users can browse `/api/listings`, read categories, and still receive protected responses on write paths. Add a new `RatingControllerTest` under `com.tradingplatform.transaction.controller` that proves public rating reads are anonymous while rating creation and `can-rate` stay authenticated. Use the seeded/test-created data already present in the suite; do not add mock or fake success paths.</action>
  <verify>
    <automated>cd backend && mvn -q -Dtest=ContentControllerTest,ListingControllerIT,RatingControllerTest test</automated>
  </verify>
  <done>The backend test suite explicitly captures the anonymous discovery and public rating contract that Phase 08 is repairing.</done>
</task>

<task type="auto">
  <name>Task 2: Align Spring Security with public discovery reads and authenticated writes</name>
  <read_first>
    - backend/src/main/java/com/tradingplatform/config/SecurityConfig.java
    - backend/src/main/java/com/tradingplatform/listing/controller/ListingController.java
    - backend/src/main/java/com/tradingplatform/content/controller/ContentController.java
    - backend/src/main/java/com/tradingplatform/transaction/controller/RatingController.java
    - backend/src/test/java/com/tradingplatform/content/controller/ContentControllerTest.java
    - backend/src/test/java/com/tradingplatform/listing/controller/ListingControllerIT.java
    - backend/src/test/java/com/tradingplatform/transaction/controller/RatingControllerTest.java
  </read_first>
  <files>backend/src/main/java/com/tradingplatform/config/SecurityConfig.java</files>
  <acceptance_criteria>
    - `SecurityConfig.java` contains GET `requestMatchers` for `/api/listings`, `/api/listings/categories`, `/api/listings/categories/**`, `/api/content/**`, and `/api/ratings/users/**`.
    - `SecurityConfig.java` does not add blanket `permitAll` rules for `/api/listings/**` or `/api/ratings/**`.
    - `SecurityConfig.java` still contains `.requestMatchers(\"/api/users/me\").authenticated()`.
    - `cd backend && mvn -q -Dtest=ContentControllerTest,ListingControllerIT,RatingControllerTest test` passes after the matcher changes.
  </acceptance_criteria>
  <action>Update `SecurityConfig` so anonymous users can call only the read endpoints that power public browse, content, and public rating surfaces. Use method-scoped matchers for GET routes instead of broad path-wide `permitAll`, and keep listing mutations, rating submission, and `can-rate` behind authentication. Preserve the existing public profile and upload rules, and keep the explicit 401 entry point behavior unchanged.</action>
  <verify>
    <automated>cd backend && mvn -q -Dtest=ContentControllerTest,ListingControllerIT,RatingControllerTest test</automated>
  </verify>
  <done>Anonymous discovery and public rating APIs return successful read responses, while all write paths remain protected.</done>
</task>

</tasks>

<verification>
Run the focused backend controller suite after each task. The final green run must prove anonymous GET access for browse, content, and public rating reads without broadening write permissions.
</verification>

<success_criteria>
The backend no longer returns 401 for anonymous browse, category, content, or public rating reads, and the authorization surface is pinned by real integration tests instead of source-string assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-public-discovery-access-and-profile-surface-integration-repair/08-01-SUMMARY.md`
</output>
