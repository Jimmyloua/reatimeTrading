---
phase: 06-chat-presence-reliability-multi-conversation-seller-status-sync-responsive-message-layout-and-redis-backed-realtime-optimization
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - frontend/src/pages/MessagesPage.tsx
  - frontend/src/components/chat/ConversationList.tsx
  - frontend/src/components/chat/ConversationItem.tsx
  - frontend/src/components/chat/ChatView.tsx
  - frontend/src/components/chat/MessageBubble.tsx
  - frontend/src/components/chat/MessageInput.tsx
  - frontend/src/tests/messages-responsive-layout.test.tsx
autonomous: false
requirements:
  - P6-04
must_haves:
  truths:
    - "Desktop users can scan conversations and the active thread side by side."
    - "Mobile users can move between list and thread views in one route with an explicit back control."
    - "Message bubbles, timestamps, and the sticky composer remain readable without horizontal scrolling."
  artifacts:
    - path: "frontend/src/pages/MessagesPage.tsx"
      provides: "Responsive messages shell with desktop/tablet/mobile route behavior"
    - path: "frontend/src/components/chat/MessageBubble.tsx"
      provides: "Responsive bubble widths and content wrapping"
    - path: "frontend/src/components/chat/MessageInput.tsx"
      provides: "Sticky mobile composer with degraded-mode helper copy"
    - path: "frontend/src/tests/messages-responsive-layout.test.tsx"
      provides: "Automated proof of shell mode switching and mobile navigation"
  key_links:
    - from: "frontend/src/pages/MessagesPage.tsx"
      to: "frontend/src/components/chat/ConversationList.tsx"
      via: "desktop/tablet sidebar and mobile list mode"
      pattern: "Active chats|Back to conversations"
    - from: "frontend/src/pages/MessagesPage.tsx"
      to: "frontend/src/components/chat/ChatView.tsx"
      via: "selected conversation and mobile thread mode"
      pattern: "conversation"
    - from: "frontend/src/components/chat/MessageBubble.tsx"
      to: "frontend/src/components/chat/MessageInput.tsx"
      via: "readable thread layout with sticky composer"
      pattern: "max-w-|sticky"
---

<objective>
Finish the user-facing messages shell so Phase 6 behavior is reliable and readable on desktop, tablet, and mobile, then close with a focused human verification gate.

Purpose: Shared presence and reconnect logic are only useful if the `/messages` route presents them clearly across responsive layouts and real user navigation.
Output: Responsive messages UI, responsive-layout tests, and a blocking human verification checklist.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-chat-presence-reliability-multi-conversation-seller-status-sync-responsive-message-layout-and-redis-backed-realtime-optimization/06-UI-SPEC.md
@frontend/src/pages/MessagesPage.tsx
@frontend/src/components/chat/ConversationList.tsx
@frontend/src/components/chat/ConversationItem.tsx
@frontend/src/components/chat/ChatView.tsx
@frontend/src/components/chat/MessageBubble.tsx
@frontend/src/components/chat/MessageInput.tsx
@frontend/src/tests/messages-responsive-layout.test.tsx
@.planning/phases/05-notification-detail-actions-quick-notification-settings-and-seller-chat-entry-from-listings/05-02-SUMMARY.md

<interfaces>
From the UI contract for `/messages`:
```text
Desktop >= 1024px: two-pane, sidebar width 320px
Tablet 768px-1023px: two-pane, sidebar width 280px
Mobile <= 767px: single-pane, explicit "Back to conversations" control, sticky composer
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the responsive `/messages` shell and message layout per P6-04</name>
  <read_first>
    - frontend/src/pages/MessagesPage.tsx
    - frontend/src/components/chat/ConversationList.tsx
    - frontend/src/components/chat/ConversationItem.tsx
    - frontend/src/components/chat/ChatView.tsx
    - frontend/src/components/chat/MessageBubble.tsx
    - frontend/src/components/chat/MessageInput.tsx
    - frontend/src/tests/messages-responsive-layout.test.tsx
    - .planning/phases/06-chat-presence-reliability-multi-conversation-seller-status-sync-responsive-message-layout-and-redis-backed-realtime-optimization/06-UI-SPEC.md
  </read_first>
  <files>frontend/src/pages/MessagesPage.tsx, frontend/src/components/chat/ConversationList.tsx, frontend/src/components/chat/ConversationItem.tsx, frontend/src/components/chat/ChatView.tsx, frontend/src/components/chat/MessageBubble.tsx, frontend/src/components/chat/MessageInput.tsx, frontend/src/tests/messages-responsive-layout.test.tsx</files>
  <acceptance_criteria>
    - `MessagesPage.tsx` contains a `320px` desktop sidebar width and a `280px` tablet sidebar width.
    - `MessagesPage.tsx` contains the exact mobile back-control text or aria-label `Back to conversations`.
    - `MessageBubble.tsx` contains a desktop `70%` width limit and a mobile `85%` width limit plus word-wrapping classes that prevent horizontal scrolling.
    - `MessageInput.tsx` contains sticky mobile composer behavior and safe-area bottom padding.
    - `messages-responsive-layout.test.tsx` passes.
  </acceptance_criteria>
  <action>Update `MessagesPage.tsx` to implement the UI contract exactly: desktop `>= 1024px` uses a two-pane layout with a fixed `320px` conversation list, tablet `768px-1023px` keeps two panes with a fixed `280px` list, and mobile `<= 767px` switches to a single-pane route state where the conversation list is the default and selecting a thread replaces it with the active thread view. Add a visible or aria-labeled control `Back to conversations` in the mobile thread header that returns to the list mode. Update `MessageBubble.tsx` so own and received bubbles still align right and left respectively, with `max-width: 70%` on desktop, `max-width: 85%` on mobile, and explicit long-word or URL wrapping to prevent horizontal overflow. Update `MessageInput.tsx` so the composer stays sticky to the bottom edge on mobile, includes safe-area padding, and preserves the degraded helper copy above the controls. Keep the existing `/messages?conversation={id}` deep-link behavior intact while changing only presentation and route-mode handling.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/tests/messages-responsive-layout.test.tsx src/tests/messages-page-routing.test.tsx</automated>
  </verify>
  <done>The messages shell matches the approved responsive contract on desktop, tablet, and mobile without breaking deep-linked conversation routing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 6 seller presence and responsive messaging flows end to end</name>
  <read_first>
    - .planning/phases/06-chat-presence-reliability-multi-conversation-seller-status-sync-responsive-message-layout-and-redis-backed-realtime-optimization/06-UI-SPEC.md
    - .planning/phases/06-chat-presence-reliability-multi-conversation-seller-status-sync-responsive-message-layout-and-redis-backed-realtime-optimization/06-VALIDATION.md
    - frontend/src/pages/MessagesPage.tsx
    - frontend/src/components/chat/ChatView.tsx
    - frontend/src/components/chat/ConversationItem.tsx
    - frontend/src/components/chat/MessageInput.tsx
  </read_first>
  <files>frontend/src/pages/MessagesPage.tsx, frontend/src/components/chat/ChatView.tsx, frontend/src/components/chat/ConversationItem.tsx, frontend/src/components/chat/MessageInput.tsx</files>
  <acceptance_criteria>
    - Automated tests for `chat-presence-sync`, `chat-realtime-fallback`, `messages-responsive-layout`, and `messages-page-routing` are green before the checkpoint starts.
    - The UI contains the exact copy `Seller online`, `Status updating`, and `Back to conversations`.
    - The mobile thread view exposes one explicit back control and one sticky composer.
  </acceptance_criteria>
  <action>Claude should complete all automation first, then pause for human verification because the remaining risk is responsive behavior, real browser reconnect UX, and cross-row visual consistency during live updates.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/tests/chat-presence-sync.test.tsx src/tests/chat-realtime-fallback.test.tsx src/tests/messages-responsive-layout.test.tsx src/tests/messages-page-routing.test.tsx</automated>
  </verify>
  <what-built>Shared seller presence rendering, duplicate-safe realtime fallback behavior, and the responsive `/messages` shell.</what-built>
  <how-to-verify>
    1. Open `/messages` on desktop and confirm the conversation list and active thread remain visible side by side, with repeated rows for the same seller showing the same presence text as the active header.
    2. Force a websocket reconnect and confirm the seller pill keeps the last known state first, the transport helper copy changes separately, and `Status updating` only appears after the stale window expires.
    3. Resize to mobile width, open a conversation, and confirm the thread replaces the list in the same route with a visible `Back to conversations` control.
    4. Scroll a long message thread on mobile and confirm the composer stays pinned to the bottom, long URLs wrap, and no horizontal scrolling appears.
    5. While degraded transport is active, send a message and confirm it still appears once, unread counts stay correct, and reconnect restores live updates without duplicate previews.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 6 or describe the failing step.</resume-signal>
  <done>Human verification confirms that seller presence, reconnect states, and responsive thread navigation behave correctly in a running browser.</done>
</task>

</tasks>

<verification>
Finish the full frontend test set before asking for manual verification so the browser check focuses only on responsive and live-behavior risks that automation does not cover well.
</verification>

<success_criteria>
Users can manage conversations reliably on desktop and mobile, and the remaining Phase 6 risks are explicitly cleared by a human verification pass.
</success_criteria>

<output>
After completion, create `.planning/phases/06-chat-presence-reliability-multi-conversation-seller-status-sync-responsive-message-layout-and-redis-backed-realtime-optimization/06-03-SUMMARY.md`
</output>
