---
phase: 06-chat-presence-reliability-multi-conversation-seller-status-sync-responsive-message-layout-and-redis-backed-realtime-optimization
plan: 02
type: execute
wave: 2
depends_on:
  - 06-00
  - 06-01
files_modified:
  - frontend/src/types/chat.ts
  - frontend/src/stores/sellerPresenceStore.ts
  - frontend/src/stores/chatStore.ts
  - frontend/src/hooks/useConversationPresence.ts
  - frontend/src/hooks/useWebSocket.ts
  - frontend/src/hooks/useChat.ts
  - frontend/src/components/chat/ConversationItem.tsx
  - frontend/src/components/chat/ConversationList.tsx
  - frontend/src/components/chat/ChatView.tsx
  - frontend/src/tests/chat-presence-sync.test.tsx
  - frontend/src/tests/chat-realtime-fallback.test.tsx
  - frontend/src/tests/messages-page-routing.test.tsx
autonomous: true
requirements:
  - P6-02
  - P6-03
must_haves:
  truths:
    - "Every row and header for the same seller shows the same presence copy and dot color."
    - "Transport reconnecting state is displayed separately from seller availability."
    - "Duplicate websocket and REST updates do not create duplicate bubbles, unread counts, or row reordering."
  artifacts:
    - path: "frontend/src/stores/sellerPresenceStore.ts"
      provides: "Shared seller-level presence cache keyed by otherUserId"
    - path: "frontend/src/hooks/useConversationPresence.ts"
      provides: "Hook that derives per-seller presence from the shared cache"
    - path: "frontend/src/hooks/useChat.ts"
      provides: "Connection-aware realtime and fallback refresh orchestration"
    - path: "frontend/src/stores/chatStore.ts"
      provides: "Duplicate-safe message and conversation preview updates"
  key_links:
    - from: "frontend/src/hooks/useConversationPresence.ts"
      to: "frontend/src/stores/sellerPresenceStore.ts"
      via: "seller ID keyed subscription and shared cache reads"
      pattern: "otherUserId"
    - from: "frontend/src/hooks/useChat.ts"
      to: "frontend/src/stores/chatStore.ts"
      via: "dedupe by message.id and reconnect rehydrate"
      pattern: "message.id|lastMessageAt"
    - from: "frontend/src/components/chat/ChatView.tsx"
      to: "frontend/src/hooks/useConversationPresence.ts"
      via: "separate seller presence pill from transport helper copy"
      pattern: "Seller online|Status updating|Reconnecting chat now"
---

<objective>
Normalize presence and realtime state on the frontend so seller availability stays synchronized across repeated threads and message updates remain stable during reconnects or REST fallback sends.

Purpose: The existing frontend owns presence per mounted view and polls active threads continuously. Phase 6 needs seller-level state ownership and duplicate-safe reconnect behavior.
Output: Shared seller presence store, connection-aware `useChat` behavior, updated list and header rendering, and passing frontend tests for P6-02 and P6-03.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-chat-presence-reliability-multi-conversation-seller-status-sync-responsive-message-layout-and-redis-backed-realtime-optimization/06-RESEARCH.md
@.planning/phases/06-chat-presence-reliability-multi-conversation-seller-status-sync-responsive-message-layout-and-redis-backed-realtime-optimization/06-UI-SPEC.md
@frontend/src/types/chat.ts
@frontend/src/stores/chatStore.ts
@frontend/src/hooks/useConversationPresence.ts
@frontend/src/hooks/useWebSocket.ts
@frontend/src/hooks/useChat.ts
@frontend/src/components/chat/ConversationItem.tsx
@frontend/src/components/chat/ConversationList.tsx
@frontend/src/components/chat/ChatView.tsx
@frontend/src/tests/messages-page-routing.test.tsx
@.planning/phases/03-real-time-communication/03-04-SUMMARY.md
@.planning/phases/05-notification-detail-actions-quick-notification-settings-and-seller-chat-entry-from-listings/05-02-SUMMARY.md

<interfaces>
From `frontend/src/hooks/useWebSocket.ts`:
```ts
export type ConnectionState = 'connecting' | 'connected' | 'disconnected' | 'reconnecting'
```

From `frontend/src/types/chat.ts` current presence contract:
```ts
export interface PresenceUpdate {
  userId: number
  online: boolean
  lastSeenText: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create a seller-level shared presence store and stale-window hook per P6-02</name>
  <read_first>
    - frontend/src/types/chat.ts
    - frontend/src/hooks/useConversationPresence.ts
    - frontend/src/hooks/useWebSocket.ts
    - frontend/src/components/chat/ConversationItem.tsx
    - frontend/src/components/chat/ChatView.tsx
    - frontend/src/tests/chat-presence-sync.test.tsx
    - .planning/phases/06-chat-presence-reliability-multi-conversation-seller-status-sync-responsive-message-layout-and-redis-backed-realtime-optimization/06-UI-SPEC.md
  </read_first>
  <files>frontend/src/types/chat.ts, frontend/src/stores/sellerPresenceStore.ts, frontend/src/hooks/useConversationPresence.ts, frontend/src/hooks/useWebSocket.ts, frontend/src/components/chat/ConversationItem.tsx, frontend/src/components/chat/ConversationList.tsx, frontend/src/components/chat/ChatView.tsx, frontend/src/tests/chat-presence-sync.test.tsx</files>
  <acceptance_criteria>
    - `frontend/src/stores/sellerPresenceStore.ts` exists and keys state by `otherUserId`.
    - `frontend/src/types/chat.ts` includes an `updatedAt` field on `PresenceUpdate`.
    - `frontend/src/hooks/useConversationPresence.ts` contains the literal stale-copy `Status updating` and the `30000` millisecond stale-preservation window.
    - `ConversationItem.tsx` and `ChatView.tsx` both render the exact seller copy `Seller online` and `Status updating`.
    - `chat-presence-sync.test.tsx` passes.
  </acceptance_criteria>
  <action>Create `frontend/src/stores/sellerPresenceStore.ts` as the single source of presence state keyed by `otherUserId`. Store exactly `status: 'online' | 'offline' | 'stale'`, `lastSeenText`, `updatedAt`, and a one-toast-per-session guard keyed by seller ID. Extend `PresenceUpdate` in `frontend/src/types/chat.ts` to include `updatedAt`. Rewrite `useConversationPresence.ts` so it seeds state from the conversation payload, subscribes once per seller topic, preserves the last known seller state for exactly `30000` milliseconds when `connectionState` is `connecting`, `reconnecting`, or `disconnected`, and only returns `Status updating` after that window expires without a fresh presence payload. Update `ConversationItem.tsx`, `ConversationList.tsx`, and the seller-status portion of `ChatView.tsx` so repeated seller rows and the active header render the same presence copy from the shared store instead of owning local `useState` copies. Keep this task limited to seller-presence rendering and transport-to-stale mapping; duplicate message handling and fallback refresh stay in Task 2.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/tests/chat-presence-sync.test.tsx</automated>
  </verify>
  <done>The frontend owns presence by seller ID, not by mounted conversation component, and repeated seller rows stay synchronized with the active header.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make realtime updates duplicate-safe and restrict refresh polling to degraded mode per P6-03</name>
  <read_first>
    - frontend/src/stores/chatStore.ts
    - frontend/src/hooks/useChat.ts
    - frontend/src/components/chat/ChatView.tsx
    - frontend/src/tests/chat-realtime-fallback.test.tsx
    - frontend/src/tests/messages-page-routing.test.tsx
    - .planning/phases/06-chat-presence-reliability-multi-conversation-seller-status-sync-responsive-message-layout-and-redis-backed-realtime-optimization/06-RESEARCH.md
  </read_first>
  <files>frontend/src/stores/chatStore.ts, frontend/src/hooks/useChat.ts, frontend/src/components/chat/ChatView.tsx, frontend/src/tests/chat-realtime-fallback.test.tsx, frontend/src/tests/messages-page-routing.test.tsx</files>
  <acceptance_criteria>
    - `chatStore.ts` contains duplicate protection based on `message.id`.
    - `chatStore.ts` only reorders conversations when the incoming `lastMessageAt` is newer than the existing timestamp.
    - `useChat.ts` contains a reconnect rehydrate path and does not rely on an always-on `setInterval` while `connectionState === 'connected'`.
    - `ChatView.tsx` renders the exact degraded helper copy `Live chat is offline right now, but messages will still send and the conversation will keep refreshing.` above the composer.
    - `chat-realtime-fallback.test.tsx` and `messages-page-routing.test.tsx` pass.
  </acceptance_criteria>
  <action>Refactor `chatStore.ts` to make message application idempotent: keep a `Set<number>` or equivalent keyed by `message.id`, ignore duplicate message payloads, and only reorder a conversation when an incoming `createdAt` would change `lastMessageAt`. Update `useChat.ts` so websocket-connected mode subscribes to `/user/queue/messages` without polling, degraded mode performs a `10000` millisecond refresh for the active conversation only, and reconnect triggers one immediate rehydrate fetch of `chatApi.getConversation(conversationId)` plus `chatApi.getMessages(conversationId)` before falling back to interval refresh again if needed. Remove the unconditional 10-second interval from `ChatView.tsx`, keep REST fallback sends unchanged for disconnected transport, and keep the degraded helper copy above the composer instead of using a modal or full-width page banner. Do not rework seller-presence ownership in this task; consume the shared presence output from Task 1 as-is.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/tests/chat-realtime-fallback.test.tsx src/tests/messages-page-routing.test.tsx</automated>
  </verify>
  <done>Realtime updates are duplicate-safe, reconnect behavior is explicit, and polling only runs when live transport is degraded.</done>
</task>

</tasks>

<verification>
Run the shared-presence and fallback tests together after implementation so presence synchronization and duplicate handling are proven against the same store state.
</verification>

<success_criteria>
The same seller presence state is rendered consistently across the page, and reconnect or REST fallback no longer creates duplicate messages, stale unread counts, or noisy always-on polling.
</success_criteria>

<output>
After completion, create `.planning/phases/06-chat-presence-reliability-multi-conversation-seller-status-sync-responsive-message-layout-and-redis-backed-realtime-optimization/06-02-SUMMARY.md`
</output>
