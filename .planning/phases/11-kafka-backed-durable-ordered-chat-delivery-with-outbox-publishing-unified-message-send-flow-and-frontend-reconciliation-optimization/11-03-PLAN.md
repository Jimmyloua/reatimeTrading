---
phase: 11-kafka-backed-durable-ordered-chat-delivery-with-outbox-publishing-unified-message-send-flow-and-frontend-reconciliation-optimization
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - frontend/src/hooks/useChat.ts
  - frontend/src/stores/chatStore.ts
  - frontend/src/types/chat.ts
  - frontend/src/api/chatApi.ts
  - frontend/src/components/chat/MessageInput.tsx
  - frontend/src/tests/chat-message-ack-reconciliation.test.tsx
  - frontend/src/tests/chat-reconnect-catchup.test.tsx
autonomous: true
requirements:
  - CHAT-02
  - CHAT-03
  - P6-03
  - P6-04
must_haves:
  truths:
    - "Optimistic messages reconcile against persisted ack payloads without full active-thread refresh."
    - "Reconnect catch-up requests only missing messages instead of reloading whole thread state."
    - "Existing duplicate-safe message behavior stays intact after the ack-contract change."
  artifacts:
    - path: "frontend/src/hooks/useChat.ts"
      provides: "Ack reconciliation and reconnect catch-up logic"
    - path: "frontend/src/tests/chat-message-ack-reconciliation.test.tsx"
      provides: "Proof that optimistic messages are replaced by persisted messages"
    - path: "frontend/src/tests/chat-reconnect-catchup.test.tsx"
      provides: "Proof that reconnect fetches only missing messages"
  key_links:
    - from: "frontend/src/hooks/useChat.ts"
      to: "frontend/src/stores/chatStore.ts"
      via: "optimistic reconciliation"
      pattern: "clientMessageId|optimistic"
    - from: "frontend/src/hooks/useChat.ts"
      to: "frontend/src/api/chatApi.ts"
      via: "cursor catch-up"
      pattern: "afterMessageId|cursor"
---

<objective>
Update the frontend chat flow for Phase 11 so persisted acknowledgments reconcile optimistic messages directly and reconnect only catches up missing messages.

Purpose: The backend delivery refactor only pays off if the client stops refreshing full conversation state on every ack and reconnect.
Output: New ack and cursor contracts in the client, targeted optimistic reconciliation, reconnect catch-up behavior, and passing frontend tests.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-kafka-backed-durable-ordered-chat-delivery-with-outbox-publishing-unified-message-send-flow-and-frontend-reconciliation-optimization/11-CONTEXT.md
@.planning/phases/11-kafka-backed-durable-ordered-chat-delivery-with-outbox-publishing-unified-message-send-flow-and-frontend-reconciliation-optimization/11-RESEARCH.md
@.planning/phases/11-kafka-backed-durable-ordered-chat-delivery-with-outbox-publishing-unified-message-send-flow-and-frontend-reconciliation-optimization/11-VALIDATION.md
@frontend/src/hooks/useChat.ts
@frontend/src/hooks/useWebSocket.ts
@frontend/src/stores/chatStore.ts
@frontend/src/api/chatApi.ts
@frontend/src/types/chat.ts
@frontend/src/tests/chat-realtime-fallback.test.tsx
@.planning/phases/06-chat-presence-reliability-multi-conversation-seller-status-sync-responsive-message-layout-and-redis-backed-realtime-optimization/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace ack-driven full refresh with optimistic message reconciliation</name>
  <read_first>
    - frontend/src/hooks/useChat.ts
    - frontend/src/stores/chatStore.ts
    - frontend/src/types/chat.ts
    - frontend/src/tests/chat-realtime-fallback.test.tsx
    - .planning/phases/11-kafka-backed-durable-ordered-chat-delivery-with-outbox-publishing-unified-message-send-flow-and-frontend-reconciliation-optimization/11-RESEARCH.md
  </read_first>
  <files>frontend/src/hooks/useChat.ts, frontend/src/stores/chatStore.ts, frontend/src/types/chat.ts, frontend/src/tests/chat-message-ack-reconciliation.test.tsx</files>
  <acceptance_criteria>
    - `frontend/src/types/chat.ts` defines an ack shape that contains `clientMessageId`, `messageId`, `conversationId`, `status`, and `createdAt`.
    - `frontend/src/hooks/useChat.ts` no longer calls `refreshActiveConversation()` directly from the message-ack subscription.
    - `frontend/src/stores/chatStore.ts` contains logic to replace or reconcile an optimistic message using a client correlation identifier.
    - `chat-message-ack-reconciliation.test.tsx` passes.
  </acceptance_criteria>
  <action>Extend the chat client contract so optimistic sends include a client correlation identifier such as `clientMessageId`, and make the ack payload return that same identifier along with the persisted `messageId`, `conversationId`, `status`, and `createdAt`. Update `useChat.ts` so `/user/queue/message-ack` reconciles the optimistic message in place instead of calling a full active-thread refresh. Add store support to replace the temporary negative-ID optimistic row with the persisted message identity and `PERSISTED` status while preserving dedupe and preview synchronization rules already established in Phase 6. Add `chat-message-ack-reconciliation.test.tsx` to prove the optimistic row is updated in place and no full refresh path is required.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/tests/chat-message-ack-reconciliation.test.tsx</automated>
  </verify>
  <done>Sender acks become cheap local reconciliation events rather than conversation-wide refresh triggers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add reconnect catch-up using persisted message cursors</name>
  <read_first>
    - frontend/src/hooks/useChat.ts
    - frontend/src/api/chatApi.ts
    - frontend/src/stores/chatStore.ts
    - frontend/src/tests/chat-realtime-fallback.test.tsx
    - backend/src/main/java/com/tradingplatform/chat/controller/ChatController.java
  </read_first>
  <files>frontend/src/hooks/useChat.ts, frontend/src/api/chatApi.ts, frontend/src/stores/chatStore.ts, frontend/src/tests/chat-reconnect-catchup.test.tsx</files>
  <acceptance_criteria>
    - `frontend/src/api/chatApi.ts` contains a request path or params using the exact token `afterMessageId`.
    - `frontend/src/hooks/useChat.ts` contains reconnect logic that calls a catch-up request using the latest persisted message marker.
    - `frontend/src/hooks/useChat.ts` no longer reloads both conversation detail and message history unconditionally on every reconnect.
    - `chat-reconnect-catchup.test.tsx` passes.
  </acceptance_criteria>
  <action>Add a cursor-based catch-up path for reconnects using the exact request parameter `afterMessageId`. Track the latest persisted message ID for the active conversation in the chat store, and on reconnect request only messages newer than that marker instead of refetching the full thread and list by default. Keep the degraded fallback behavior for disconnected mode, but change the reconnect path so it appends only missing persisted messages and updates conversation preview metadata without a full-thread reset. Add `chat-reconnect-catchup.test.tsx` to prove reconnect appends only unseen persisted messages and preserves the visible thread state.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/tests/chat-reconnect-catchup.test.tsx src/tests/chat-message-ack-reconciliation.test.tsx</automated>
  </verify>
  <done>Reconnect behavior becomes delta-based and aligns with the new persisted-ack delivery model.</done>
</task>

</tasks>

<verification>
Preserve the duplicate-safe behavior introduced in Phase 6 while removing unnecessary full refreshes from both send and reconnect flows.
</verification>

<success_criteria>
Frontend chat remains optimistic and duplicate-safe, but acks and reconnects are now reconciliation and catch-up operations rather than broad refresh operations.
</success_criteria>

<output>
After completion, create `.planning/phases/11-kafka-backed-durable-ordered-chat-delivery-with-outbox-publishing-unified-message-send-flow-and-frontend-reconciliation-optimization/11-03-SUMMARY.md`
</output>
