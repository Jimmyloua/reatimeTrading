---
phase: 03-real-time-communication
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "Test files exist for all chat and notification components"
    - "Maven has spring-boot-starter-websocket and spring-kafka dependencies"
    - "npm has @stomp/stompjs and sockjs-client dependencies"
  artifacts:
    - path: "backend/pom.xml"
      provides: "WebSocket and Kafka dependencies"
      contains: "spring-boot-starter-websocket"
    - path: "frontend/package.json"
      provides: "STOMP client dependencies"
      contains: "@stomp/stompjs"
  key_links: []
---

<objective>
Set up test infrastructure and add required dependencies for real-time communication features.

Purpose: Nyquist compliance requires test stubs to exist before implementation begins. This plan adds the foundational dependencies (WebSocket, Kafka, STOMP) and creates test file stubs.
Output: Test stubs in place, dependencies added to pom.xml and package.json.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-real-time-communication/03-VALIDATION.md
</context>

<interfaces>
<!-- Existing patterns from codebase -->

From backend/src/main/java/com/tradingplatform/user/User.java:
```java
@Entity
@Table(name = "users")
@EntityListeners(AuditingEntityListener.class)
@Getter @Setter @NoArgsConstructor @AllArgsConstructor @Builder
public class User {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    // ... other fields
}
```

From backend/src/main/java/com/tradingplatform/listing/entity/Listing.java:
```java
@Entity
@Table(name = "listings")
@EntityListeners(AuditingEntityListener.class)
@Data @Builder @NoArgsConstructor @AllArgsConstructor
public class Listing {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    @Column(name = "user_id", nullable = false)
    private Long userId;
    // ... other fields
}
```

From backend/src/main/java/com/tradingplatform/security/JwtTokenProvider.java:
```java
@Component
public class JwtTokenProvider {
    public boolean validateToken(String token);
    public String getUserIdFromToken(String token);
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add WebSocket and Kafka dependencies to backend</name>
  <files>backend/pom.xml</files>
  <read_first>
    - d:/Java/Projects/realTimeTrading/backend/pom.xml (current dependencies)
  </read_first>
  <action>Add the following dependencies to pom.xml within the existing `&lt;dependencies&gt;` section:

```xml
<!-- WebSocket for real-time communication -->
&lt;dependency&gt;
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
    &lt;artifactId&gt;spring-boot-starter-websocket&lt;/artifactId&gt;
&lt;/dependency&gt;

<!-- Kafka for message persistence and event streaming -->
&lt;dependency&gt;
    &lt;groupId&gt;org.springframework.kafka&lt;/groupId&gt;
    &lt;artifactId&gt;spring-kafka&lt;/artifactId&gt;
&lt;/dependency&gt;
```

Place these after the existing spring-boot-starter-data-redis dependency (around line 44). Do NOT modify any other dependencies.</action>
  <verify>
    <automated>grep -c "spring-boot-starter-websocket" backend/pom.xml && grep -c "spring-kafka" backend/pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - backend/pom.xml contains "spring-boot-starter-websocket"
    - backend/pom.xml contains "spring-kafka"
    - Both dependencies are within the &lt;dependencies&gt; section
  </acceptance_criteria>
  <done>Maven has WebSocket and Kafka dependencies added.</done>
</task>

<task type="auto">
  <name>Task 2: Add STOMP client dependencies to frontend</name>
  <files>frontend/package.json</files>
  <read_first>
    - d:/Java/Projects/realTimeTrading/frontend/package.json (current dependencies)
  </read_first>
  <action>Run the following commands in the frontend directory to install WebSocket client libraries:

```bash
cd frontend && npm install @stomp/stompjs@^7.0.0 sockjs-client@^1.6.0
cd frontend && npm install -D @types/sockjs-client
```

This adds:
- @stomp/stompjs: STOMP protocol client for WebSocket
- sockjs-client: WebSocket fallback for restricted networks
- @types/sockjs-client: TypeScript type definitions</action>
  <verify>
    <automated>grep -c "@stomp/stompjs" frontend/package.json && grep -c "sockjs-client" frontend/package.json</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json contains "@stomp/stompjs" in dependencies
    - frontend/package.json contains "sockjs-client" in dependencies
    - frontend/package.json contains "@types/sockjs-client" in devDependencies
  </acceptance_criteria>
  <done>Frontend has STOMP and SockJS client dependencies installed.</done>
</task>

<task type="auto">
  <name>Task 3: Create test stub files for chat and notification modules</name>
  <files>
    backend/src/test/java/com/tradingplatform/chat/controller/ChatControllerTest.java,
    backend/src/test/java/com/tradingplatform/chat/controller/ChatWebSocketControllerTest.java,
    backend/src/test/java/com/tradingplatform/chat/service/ChatServiceTest.java,
    backend/src/test/java/com/tradingplatform/chat/repository/MessageRepositoryTest.java,
    backend/src/test/java/com/tradingplatform/notification/controller/NotificationControllerTest.java,
    backend/src/test/java/com/tradingplatform/notification/service/NotificationServiceTest.java,
    backend/src/test/java/com/tradingplatform/notification/repository/NotificationRepositoryTest.java
  </files>
  <read_first>
    - d:/Java/Projects/realTimeTrading/backend/src/test/java/com/tradingplatform/listing/controller/ListingControllerIT.java (test pattern reference)
  </read_first>
  <action>Create test stub files with placeholder test methods. Each test file should:

1. Have the correct package declaration matching its location
2. Import JUnit 5 annotations (@Test, @DisplayName, etc.)
3. Have a @DisplayName annotation describing the test class purpose
4. Contain placeholder test methods marked with @Test and @Disabled("Implementation pending")

Follow this pattern for each file:

```java
package com.tradingplatform.chat.controller;

import org.junit.jupiter.api.*;
import static org.junit.jupiter.api.Assertions.*;

@DisplayName("ChatController Tests")
class ChatControllerTest {

    @Test
    @Disabled("Implementation pending")
    @DisplayName("Should create conversation when user initiates chat")
    void testCreateConversation() {
        // Test for CHAT-01
    }

    @Test
    @Disabled("Implementation pending")
    @DisplayName("Should return conversation list for user")
    void testGetConversations() {
        // Test for CHAT-03
    }
}
```

Create similar stubs for:
- ChatControllerTest: testCreateConversation, testGetConversations (CHAT-01, CHAT-03)
- ChatWebSocketControllerTest: testSendMessage, testTypingIndicator (CHAT-02, CHAT-05)
- ChatServiceTest: testMessagePersistedBeforeDelivery, testGetConversationHistory (CHAT-04)
- MessageRepositoryTest: testFindByConversationIdOrderByCreatedAtDesc (CHAT-03)
- NotificationControllerTest: testGetNotifications, testMarkAsRead (NOTF-03, NOTF-04)
- NotificationServiceTest: testCreateMessageNotification, testCreateItemSoldNotification, testMarkAsRead (NOTF-01, NOTF-02, NOTF-04)
- NotificationRepositoryTest: testFindByUserIdOrderByCreatedAtDesc, testCountByUserIdAndReadFalse (NOTF-03)</action>
  <verify>
    <automated>ls backend/src/test/java/com/tradingplatform/chat/controller/ChatControllerTest.java backend/src/test/java/com/tradingplatform/notification/service/NotificationServiceTest.java 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/test/java/com/tradingplatform/chat/controller/ChatControllerTest.java exists
    - backend/src/test/java/com/tradingplatform/chat/controller/ChatWebSocketControllerTest.java exists
    - backend/src/test/java/com/tradingplatform/chat/service/ChatServiceTest.java exists
    - backend/src/test/java/com/tradingplatform/chat/repository/MessageRepositoryTest.java exists
    - backend/src/test/java/com/tradingplatform/notification/controller/NotificationControllerTest.java exists
    - backend/src/test/java/com/tradingplatform/notification/service/NotificationServiceTest.java exists
    - backend/src/test/java/com/tradingplatform/notification/repository/NotificationRepositoryTest.java exists
    - Each file contains @Disabled annotation (test stubs)
  </acceptance_criteria>
  <done>All 7 test stub files exist with placeholder test methods.</done>
</task>

</tasks>

<verification>
- Run `mvn compile -f backend/pom.xml` to verify dependencies resolve
- Run `npm ls @stomp/stompjs sockjs-client --prefix frontend` to verify frontend dependencies
- Test stubs compile without errors
</verification>

<success_criteria>
- backend/pom.xml contains spring-boot-starter-websocket and spring-kafka
- frontend/package.json contains @stomp/stompjs and sockjs-client
- All 7 test stub files exist with @Disabled placeholder tests
- `mvn test -f backend/pom.xml` passes (tests are disabled)
</success_criteria>

<output>
After completion, create `.planning/phases/03-real-time-communication/03-00-SUMMARY.md`
</output>