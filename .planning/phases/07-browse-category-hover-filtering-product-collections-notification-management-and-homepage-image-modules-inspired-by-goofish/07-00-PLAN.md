---
phase: 07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/tests/browse-category-hover.test.tsx
  - frontend/src/tests/homepage-modules.test.tsx
  - frontend/src/tests/notification-management.test.tsx
  - backend/src/test/java/com/tradingplatform/content/controller/ContentControllerTest.java
  - backend/src/test/java/com/tradingplatform/content/service/ContentServiceTest.java
  - backend/src/test/java/com/tradingplatform/notification/controller/NotificationControllerTest.java
autonomous: true
requirements:
  - P7-01
  - P7-02
  - P7-03
  - P7-04
  - P7-05
must_haves:
  truths:
    - "Phase 7 implementation starts from explicit failing tests for category disclosure, homepage modules, curated collections, and notification management."
    - "The test suite names the exact URL params, endpoint paths, copy, and ordering rules that later plans must satisfy."
    - "Notification management scope is pinned to filtering, grouped preferences, and read actions instead of delete or archive behavior."
  artifacts:
    - path: "frontend/src/tests/browse-category-hover.test.tsx"
      provides: "Frontend proof for preview-only hover state versus committed category filters"
    - path: "frontend/src/tests/homepage-modules.test.tsx"
      provides: "Frontend proof for server-driven homepage modules and browse routing"
    - path: "frontend/src/tests/notification-management.test.tsx"
      provides: "Frontend proof for notification tabs, filters, grouped preferences, and mark-visible-as-read"
    - path: "backend/src/test/java/com/tradingplatform/content/controller/ContentControllerTest.java"
      provides: "Backend proof for active ordered homepage and collection payloads"
    - path: "backend/src/test/java/com/tradingplatform/content/service/ContentServiceTest.java"
      provides: "Backend proof for collection membership filtering and fallback behavior"
    - path: "backend/src/test/java/com/tradingplatform/notification/controller/NotificationControllerTest.java"
      provides: "Backend proof for filtered notification reads and query-param contract"
  key_links:
    - from: "frontend/src/tests/browse-category-hover.test.tsx"
      to: "frontend/src/pages/BrowseListingsPage.tsx"
      via: "preview state versus search-param commits"
      pattern: "categoryId|collection"
    - from: "frontend/src/tests/homepage-modules.test.tsx"
      to: "frontend/src/pages/HomePage.tsx"
      via: "module rendering and CTA routing"
      pattern: "/listings\\?"
    - from: "backend/src/test/java/com/tradingplatform/content/controller/ContentControllerTest.java"
      to: "backend/src/main/java/com/tradingplatform/content/controller/ContentController.java"
      via: "GET /api/content/homepage and GET /api/content/collections/{slug}"
      pattern: "/api/content/homepage|/api/content/collections"
    - from: "backend/src/test/java/com/tradingplatform/notification/controller/NotificationControllerTest.java"
      to: "backend/src/main/java/com/tradingplatform/notification/controller/NotificationController.java"
      via: "tab/type filters and visible-read endpoint"
      pattern: "tab|types|read-visible"
---

<objective>
Create the Wave 0 failing tests for Phase 7 before schema, API, and UI work begins.

Purpose: The phase adds URL-backed browse state, server-driven content, and notification-management behavior across multiple surfaces. Explicit red tests prevent ambiguous implementation and keep later plans inside the researched scope.
Output: Frontend and backend test files describing the exact contracts for P7-01 through P7-05.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-RESEARCH.md
@.planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-VALIDATION.md
@frontend/src/pages/BrowseListingsPage.tsx
@frontend/src/pages/NotificationsPage.tsx
@frontend/src/components/notifications/NotificationDropdown.tsx
@backend/src/main/java/com/tradingplatform/notification/controller/NotificationController.java
@backend/src/test/java/com/tradingplatform/notification/controller/NotificationControllerTest.java

<interfaces>
From `frontend/src/types/listing.ts`:
```ts
export interface ListingSearchRequest {
  query?: string
  categoryId?: number
  minPrice?: number
  maxPrice?: number
  conditions?: Condition[]
  city?: string
  region?: string
  page?: number
  size?: number
  sort?: string
}
```

From `frontend/src/types/notification.ts`:
```ts
export interface NotificationPreferences {
  newMessageEnabled: boolean
  itemSoldEnabled: boolean
  transactionUpdateEnabled: boolean
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add frontend red tests for browse disclosure, homepage modules, and notification management</name>
  <read_first>
    - frontend/src/pages/BrowseListingsPage.tsx
    - frontend/src/App.tsx
    - frontend/src/pages/NotificationsPage.tsx
    - frontend/src/components/notifications/NotificationDropdown.tsx
    - frontend/src/components/notifications/NotificationList.tsx
    - .planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-RESEARCH.md
    - .planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-VALIDATION.md
  </read_first>
  <files>frontend/src/tests/browse-category-hover.test.tsx, frontend/src/tests/homepage-modules.test.tsx, frontend/src/tests/notification-management.test.tsx</files>
  <acceptance_criteria>
    - `frontend/src/tests/browse-category-hover.test.tsx` exists and contains the exact query-param keys `categoryId` and `collection`.
    - `frontend/src/tests/browse-category-hover.test.tsx` contains assertions proving hover or focus preview does not commit `categoryId` until an explicit click, Enter key, or disclosure action occurs.
    - `frontend/src/tests/homepage-modules.test.tsx` exists and contains module types `hero`, `image_tiles`, `collection_row`, and `category_spotlight`.
    - `frontend/src/tests/homepage-modules.test.tsx` contains browse destinations `/listings?categoryId=` and `/listings?collection=`.
    - `frontend/src/tests/notification-management.test.tsx` exists and contains the exact query-param keys `tab`, `types`, `page`, and the action copy `Mark visible as read`.
  </acceptance_criteria>
  <action>Create three failing frontend tests. In `browse-category-hover.test.tsx`, define a disclosure-navigation contract where local preview state can open categories on hover or focus but `setSearchParams` is only committed when the user clicks a category link, presses Enter on a focused option, or activates a disclosure button; the URL must keep `categoryId` unchanged during preview-only movement. In `homepage-modules.test.tsx`, define server-driven rendering for `hero`, `image_tiles`, `collection_row`, and `category_spotlight` modules and assert that module CTA clicks navigate to `/listings?categoryId={id}` or `/listings?collection={slug}` exactly. In `notification-management.test.tsx`, define the notifications page contract with URL-backed `tab=all|unread`, comma-separated `types`, grouped preference controls, and a `Mark visible as read` action that only affects the currently filtered rows.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/tests/browse-category-hover.test.tsx src/tests/homepage-modules.test.tsx src/tests/notification-management.test.tsx</automated>
  </verify>
  <done>Frontend Wave 0 tests exist and fail against the current implementation while describing the exact browse, homepage, and notification-management contracts for Phase 7.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add backend red tests for content delivery and filtered notification management</name>
  <read_first>
    - backend/src/main/java/com/tradingplatform/notification/controller/NotificationController.java
    - backend/src/main/java/com/tradingplatform/notification/service/NotificationService.java
    - backend/src/main/java/com/tradingplatform/notification/repository/NotificationRepository.java
    - backend/src/test/java/com/tradingplatform/notification/controller/NotificationControllerTest.java
    - .planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-RESEARCH.md
    - .planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-VALIDATION.md
  </read_first>
  <files>backend/src/test/java/com/tradingplatform/content/controller/ContentControllerTest.java, backend/src/test/java/com/tradingplatform/content/service/ContentServiceTest.java, backend/src/test/java/com/tradingplatform/notification/controller/NotificationControllerTest.java</files>
  <acceptance_criteria>
    - `backend/src/test/java/com/tradingplatform/content/controller/ContentControllerTest.java` exists and contains the exact endpoint paths `/api/content/homepage` and `/api/content/collections/featured-cameras`.
    - `backend/src/test/java/com/tradingplatform/content/service/ContentServiceTest.java` exists and contains the enum-like values `AVAILABLE`, `hero`, `image_tiles`, `collection_row`, and `category_spotlight`.
    - `backend/src/test/java/com/tradingplatform/notification/controller/NotificationControllerTest.java` contains assertions for query params named `tab` and `types`.
    - `backend/src/test/java/com/tradingplatform/notification/controller/NotificationControllerTest.java` contains assertions for the endpoint path `/api/notifications/read-visible`.
    - The backend test files contain ordering assertions against `displayOrder`.
  </acceptance_criteria>
  <action>Create failing backend tests before implementation. Add `ContentControllerTest.java` that asserts `GET /api/content/homepage` returns only active modules and items ordered by `displayOrder`, and `GET /api/content/collections/{slug}` returns only active collections with only `AVAILABLE` listing items. Add `ContentServiceTest.java` that asserts unavailable or imageless collection members are filtered or replaced with a collection-level fallback image, and that homepage module types `hero`, `image_tiles`, `collection_row`, and `category_spotlight` are preserved in response order. Extend `NotificationControllerTest.java` with failing coverage for `GET /api/notifications?tab=unread&types=NEW_MESSAGE,ITEM_SOLD&page=0&size=20` and `PATCH /api/notifications/read-visible?tab=unread&types=NEW_MESSAGE`, proving the backend contract stays limited to filtering and read actions without delete or archive semantics.</action>
  <verify>
    <automated>cd backend && mvn -q -Dtest=ContentControllerTest,ContentServiceTest,NotificationControllerTest test</automated>
  </verify>
  <done>Backend Wave 0 tests exist and fail while defining the exact content and notification-management contracts that Phase 7 implementation must satisfy.</done>
</task>

</tasks>

<verification>
Wave 0 is complete when all six test files exist, each command runs, and the failures point to missing Phase 7 implementation instead of malformed tests.
</verification>

<success_criteria>
Phase 7 implementation work proceeds from explicit failing tests that encode URL params, endpoint paths, display ordering, and read-only notification-management scope.
</success_criteria>

<output>
After completion, create `.planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-00-SUMMARY.md`
</output>
