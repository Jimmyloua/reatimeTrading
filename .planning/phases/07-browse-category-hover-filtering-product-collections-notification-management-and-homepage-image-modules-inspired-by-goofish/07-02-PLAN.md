---
phase: 07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish
plan: 02
type: execute
wave: 1
depends_on:
  - 07-00
files_modified:
  - backend/src/main/java/com/tradingplatform/notification/repository/NotificationRepository.java
  - backend/src/main/java/com/tradingplatform/notification/service/NotificationService.java
  - backend/src/main/java/com/tradingplatform/notification/controller/NotificationController.java
  - backend/src/test/java/com/tradingplatform/notification/controller/NotificationControllerTest.java
  - backend/src/test/java/com/tradingplatform/notification/service/NotificationServiceTest.java
autonomous: true
requirements:
  - P7-05
must_haves:
  truths:
    - "Notification management supports URL-backed filtering by unread state and type without introducing delete or archive semantics."
    - "Users can mark only the currently visible filtered notification set as read."
    - "Notification read actions keep the existing history and preference model intact."
  artifacts:
    - path: "backend/src/main/java/com/tradingplatform/notification/controller/NotificationController.java"
      provides: "Filtered notification listing and read-visible API contract"
    - path: "backend/src/main/java/com/tradingplatform/notification/service/NotificationService.java"
      provides: "Filtered query and visible-read behavior"
    - path: "backend/src/main/java/com/tradingplatform/notification/repository/NotificationRepository.java"
      provides: "Filtered database access by unread status and types"
    - path: "backend/src/test/java/com/tradingplatform/notification/controller/NotificationControllerTest.java"
      provides: "Automated proof for query-param contract and visible-read endpoint"
  key_links:
    - from: "backend/src/main/java/com/tradingplatform/notification/controller/NotificationController.java"
      to: "backend/src/main/java/com/tradingplatform/notification/service/NotificationService.java"
      via: "GET /api/notifications and PATCH /api/notifications/read-visible"
      pattern: "read-visible|getNotifications"
    - from: "backend/src/main/java/com/tradingplatform/notification/service/NotificationService.java"
      to: "backend/src/main/java/com/tradingplatform/notification/repository/NotificationRepository.java"
      via: "tab/type filtering and visible-read selection"
      pattern: "find|mark"
---

<objective>
Extend the backend notification API from simple history to filtered management and filtered read actions.

Purpose: The frontend notification center needs a canonical server contract for tabs, type filters, and mark-visible-as-read behavior. This plan keeps Phase 7 scope limited to filtering, grouped preferences, and read actions.
Output: Updated notification repository/service/controller behavior and passing backend tests for P7-05.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-RESEARCH.md
@.planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-VALIDATION.md
@backend/src/main/java/com/tradingplatform/notification/controller/NotificationController.java
@backend/src/main/java/com/tradingplatform/notification/service/NotificationService.java
@backend/src/main/java/com/tradingplatform/notification/repository/NotificationRepository.java
@backend/src/main/java/com/tradingplatform/notification/entity/NotificationType.java
@backend/src/test/java/com/tradingplatform/notification/controller/NotificationControllerTest.java
@backend/src/test/java/com/tradingplatform/notification/service/NotificationServiceTest.java

<interfaces>
Existing endpoint:
```text
GET /api/notifications?page=0&size=20
PATCH /api/notifications/{id}/read
PATCH /api/notifications/read-all
```

Phase 7 additions to implement:
```text
GET /api/notifications?tab=all|unread&types=NEW_MESSAGE,ITEM_SOLD
PATCH /api/notifications/read-visible?tab=all|unread&types=NEW_MESSAGE,ITEM_SOLD
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add filtered notification queries and mark-visible-as-read behavior</name>
  <read_first>
    - backend/src/main/java/com/tradingplatform/notification/repository/NotificationRepository.java
    - backend/src/main/java/com/tradingplatform/notification/service/NotificationService.java
    - backend/src/main/java/com/tradingplatform/notification/entity/NotificationType.java
    - backend/src/test/java/com/tradingplatform/notification/service/NotificationServiceTest.java
    - .planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-RESEARCH.md
  </read_first>
  <files>backend/src/main/java/com/tradingplatform/notification/repository/NotificationRepository.java, backend/src/main/java/com/tradingplatform/notification/service/NotificationService.java, backend/src/test/java/com/tradingplatform/notification/service/NotificationServiceTest.java</files>
  <acceptance_criteria>
    - `NotificationService.java` contains filter parameters named `tab` and `types`.
    - `NotificationService.java` contains a method named `markVisibleAsRead`.
    - `NotificationRepository.java` contains filtered query support for unread state and notification types.
    - `NotificationServiceTest.java` contains assertions covering `tab=all`, `tab=unread`, and the type list `NEW_MESSAGE,ITEM_SOLD`.
    - No backend file added by this task contains the strings `delete` or `archive` for notification-management behavior.
  </acceptance_criteria>
  <action>Extend notification management without changing retention semantics. In `NotificationService`, add filtered list support that accepts `tab` with only `all` or `unread`, accepts an optional comma-separated list of `NotificationType` values in `types`, and returns the same `Page<NotificationResponse>` shape already used by the controller. Add `markVisibleAsRead(Long userId, String tab, List<NotificationType> types)` that selects only the records visible under the same filter and marks them read. Update `NotificationRepository` with queries that can filter by `userId`, optional unread-only state, and an optional `IN (:types)` clause. Keep `markAllAsRead` and single-item `markAsRead` intact; do not introduce delete, archive, or retention-policy changes.</action>
  <verify>
    <automated>cd backend && mvn -q -Dtest=NotificationServiceTest test</automated>
  </verify>
  <done>The service layer can list and mark read only the filtered visible notification set while preserving the existing notification model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose filtered notification-management endpoints in the controller</name>
  <read_first>
    - backend/src/main/java/com/tradingplatform/notification/controller/NotificationController.java
    - backend/src/main/java/com/tradingplatform/notification/service/NotificationService.java
    - backend/src/test/java/com/tradingplatform/notification/controller/NotificationControllerTest.java
    - backend/src/test/java/com/tradingplatform/notification/service/NotificationServiceTest.java
  </read_first>
  <files>backend/src/main/java/com/tradingplatform/notification/controller/NotificationController.java, backend/src/test/java/com/tradingplatform/notification/controller/NotificationControllerTest.java</files>
  <acceptance_criteria>
    - `NotificationController.java` accepts request params `tab`, `types`, `page`, and `size` on `GET /api/notifications`.
    - `NotificationController.java` contains `@PatchMapping("/read-visible")`.
    - `NotificationControllerTest.java` contains the literal request `/api/notifications/read-visible`.
    - `NotificationControllerTest.java` proves `PATCH /api/notifications/read-visible?tab=unread&types=NEW_MESSAGE` does not mark `ITEM_SOLD` notifications as read.
    - `NotificationControllerTest.java` still contains coverage for `PATCH /api/notifications/read-all`.
  </acceptance_criteria>
  <action>Update `NotificationController` so `GET /api/notifications` accepts optional `tab` and `types` query params while preserving existing pagination params `page` and `size`. Add `PATCH /api/notifications/read-visible` with the same filter params and return `200 OK` after marking only the filtered visible set read. Keep the response shape for `GET /api/notifications` as `Page<NotificationResponse>`. Harden `NotificationControllerTest` so it covers mixed-type data and verifies that `tab=unread&types=NEW_MESSAGE` leaves unread `ITEM_SOLD` rows untouched, and that `tab=all` plus no `types` still marks the whole visible page read when explicitly requested.</action>
  <verify>
    <automated>cd backend && mvn -q -Dtest=NotificationControllerTest,NotificationServiceTest test</automated>
  </verify>
  <done>The backend exposes the exact filtered notification-management contract needed by the frontend notification center, with read actions constrained to the current filter.</done>
</task>

</tasks>

<verification>
Run the focused notification backend suite and make sure existing unread count, single-item read, and read-all behavior remain green alongside the new filtered endpoints.
</verification>

<success_criteria>
Phase 7 notification management has a backend API for URL-backed filters and filtered read actions, with no scope drift into delete or archive semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-02-SUMMARY.md`
</output>
