---
phase: 07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish
plan: 06
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - backend/src/main/java/com/tradingplatform/content/dto/CuratedCollectionResponse.java
  - backend/src/main/java/com/tradingplatform/content/dto/HomepageResponse.java
  - backend/src/main/java/com/tradingplatform/content/service/ContentService.java
  - backend/src/main/java/com/tradingplatform/content/controller/ContentController.java
  - backend/src/test/java/com/tradingplatform/content/controller/ContentControllerTest.java
  - frontend/src/api/contentApi.ts
  - frontend/src/types/content.ts
autonomous: true
requirements:
  - P7-03
must_haves:
  truths:
    - "The backend content foundation is exposed as stable homepage and collection APIs."
    - "Frontend content client types exist before homepage and browse UI plans consume them."
    - "Homepage and collection payloads preserve active ordering and listing-card fields for the frontend."
  artifacts:
    - path: "backend/src/main/java/com/tradingplatform/content/controller/ContentController.java"
      provides: "Read-only homepage and collection endpoints"
    - path: "backend/src/main/java/com/tradingplatform/content/dto/HomepageResponse.java"
      provides: "Ordered homepage module DTO contract"
    - path: "backend/src/main/java/com/tradingplatform/content/dto/CuratedCollectionResponse.java"
      provides: "Collection DTO contract with listing-card items"
    - path: "frontend/src/api/contentApi.ts"
      provides: "Frontend client for content endpoints"
    - path: "frontend/src/types/content.ts"
      provides: "Shared frontend content payload types"
    - path: "backend/src/test/java/com/tradingplatform/content/controller/ContentControllerTest.java"
      provides: "Automated proof for active ordered content API payloads"
  key_links:
    - from: "backend/src/main/java/com/tradingplatform/content/controller/ContentController.java"
      to: "backend/src/main/java/com/tradingplatform/content/service/ContentService.java"
      via: "GET /api/content/homepage and /api/content/collections/{slug}"
      pattern: "getHomepage|getCollectionBySlug"
    - from: "frontend/src/api/contentApi.ts"
      to: "backend/src/main/java/com/tradingplatform/content/controller/ContentController.java"
      via: "frontend API client methods"
      pattern: "/api/content/homepage|/api/content/collections"
---

<objective>
Expose the Phase 7 content foundation as REST payloads and frontend client contracts.

Purpose: The schema and ordering logic are now isolated in the foundation plan. This plan turns that foundation into stable homepage and collection APIs plus the frontend client/types that later UI plans consume.
Output: Content DTOs, content controller, DTO-aware service mapping, controller tests, and frontend content API/types.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-RESEARCH.md
@backend/src/main/java/com/tradingplatform/content/service/ContentService.java
@backend/src/test/java/com/tradingplatform/content/controller/ContentControllerTest.java
@backend/src/test/java/com/tradingplatform/content/service/ContentServiceTest.java
@frontend/src/api/listingApi.ts
@frontend/src/types/listing.ts

<interfaces>
Endpoints to expose in this plan:
```text
GET /api/content/homepage
GET /api/content/collections/{slug}
```

Frontend listing-card fields to preserve:
```text
id, title, price, condition, primaryImageUrl, categoryId, categoryName, createdAt
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DTO mapping and content controller endpoints on top of the foundation service</name>
  <read_first>
    - backend/src/main/java/com/tradingplatform/content/service/ContentService.java
    - backend/src/test/java/com/tradingplatform/content/controller/ContentControllerTest.java
    - backend/src/test/java/com/tradingplatform/content/service/ContentServiceTest.java
    - backend/src/main/java/com/tradingplatform/listing/service/ListingService.java
  </read_first>
  <files>backend/src/main/java/com/tradingplatform/content/dto/CuratedCollectionResponse.java, backend/src/main/java/com/tradingplatform/content/dto/HomepageResponse.java, backend/src/main/java/com/tradingplatform/content/service/ContentService.java, backend/src/main/java/com/tradingplatform/content/controller/ContentController.java, backend/src/test/java/com/tradingplatform/content/controller/ContentControllerTest.java</files>
  <acceptance_criteria>
    - `ContentController.java` contains `@RequestMapping("/api/content")`, `@GetMapping("/homepage")`, and `@GetMapping("/collections/{slug}")`.
    - `ContentService.java` contains DTO-returning methods named `getHomepage()` and `getCollectionBySlug(String slug)`.
    - `CuratedCollectionResponse.java` contains the fields `slug`, `title`, `subtitle`, `description`, `coverImageUrl`, `targetType`, `targetValue`, and `items`.
    - `HomepageResponse.java` contains ordered modules and item payload fields that include `moduleType`, `title`, `subtitle`, `imageUrl`, `linkType`, `linkValue`, and `displayOrder`.
    - `ContentControllerTest.java` passes.
  </acceptance_criteria>
  <action>Build the REST API on top of the foundation service from Plan 07-01. Add `CuratedCollectionResponse.java` and `HomepageResponse.java` DTOs, then update `ContentService.java` so `getHomepage()` and `getCollectionBySlug(String slug)` map the already-filtered foundation results into API payloads. Add `ContentController.java` with `GET /api/content/homepage` and `GET /api/content/collections/{slug}`. Preserve active ordering, preserve the four module types `hero`, `image_tiles`, `collection_row`, and `category_spotlight`, and reuse the existing listing-card field shape so the frontend does not need a second card contract.</action>
  <verify>
    <automated>cd backend && mvn -q -Dtest=ContentControllerTest test</automated>
  </verify>
  <done>The backend exposes stable content endpoints backed by the foundation ordering and filtering logic.</done>
</task>

<task type="auto">
  <name>Task 2: Create the frontend content client and type contracts for later UI plans</name>
  <read_first>
    - frontend/src/api/listingApi.ts
    - frontend/src/types/listing.ts
    - backend/src/main/java/com/tradingplatform/content/dto/CuratedCollectionResponse.java
    - backend/src/main/java/com/tradingplatform/content/dto/HomepageResponse.java
  </read_first>
  <files>frontend/src/api/contentApi.ts, frontend/src/types/content.ts</files>
  <acceptance_criteria>
    - `frontend/src/api/contentApi.ts` exists and contains methods `getHomepage()` and `getCollection(slug: string)`.
    - `frontend/src/types/content.ts` exists and contains module types `hero`, `image_tiles`, `collection_row`, and `category_spotlight`.
    - `frontend/src/types/content.ts` contains item link types `category`, `listing`, `collection`, and `route`.
    - `07-03-PLAN.md` and later frontend plans can consume these files without redefining content DTOs locally.
  </acceptance_criteria>
  <action>Create `frontend/src/types/content.ts` to mirror the backend content payload shape and `frontend/src/api/contentApi.ts` to call `/api/content/homepage` and `/api/content/collections/{slug}` through the existing API client pattern. Keep these files contract-only: no homepage rendering, no browse routing logic, and no extra UI work in this plan. The goal is to hand the later frontend plans stable imports rather than forcing them to rediscover DTO shapes.</action>
  <verify>
    <automated>cd backend && mvn -q -Dtest=ContentControllerTest test</automated>
  </verify>
  <done>The frontend has stable content client/type contracts ready before homepage and browse UI implementation begins.</done>
</task>

</tasks>

<verification>
Run the controller-focused backend test slice and confirm the frontend contract files mirror the DTO fields that the controller returns.
</verification>

<success_criteria>
Homepage and collection APIs exist with stable DTOs, and the frontend has importable content client/type contracts before UI implementation starts.
</success_criteria>

<output>
After completion, create `.planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-06-SUMMARY.md`
</output>
