---
phase: 07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish
plan: 04
type: execute
wave: 2
depends_on:
  - 07-00
  - 07-02
files_modified:
  - frontend/src/api/notificationApi.ts
  - frontend/src/hooks/useNotifications.ts
  - frontend/src/stores/notificationStore.ts
  - frontend/src/pages/NotificationsPage.tsx
  - frontend/src/components/notifications/NotificationDropdown.tsx
  - frontend/src/components/notifications/NotificationList.tsx
  - frontend/src/components/notifications/NotificationManagementToolbar.tsx
  - frontend/src/components/notifications/NotificationPreferenceGroups.tsx
  - frontend/src/tests/notification-management.test.tsx
  - frontend/src/tests/notification-preferences.test.tsx
autonomous: true
requirements:
  - P7-04
  - P7-05
must_haves:
  truths:
    - "The notification page uses URL-backed tabs and type filters instead of page-local state only."
    - "Unread counts and read actions stay synchronized between the bell dropdown and the notification page."
    - "Notification management remains limited to filtering, grouped preferences, and read actions."
  artifacts:
    - path: "frontend/src/pages/NotificationsPage.tsx"
      provides: "URL-backed notification management center"
    - path: "frontend/src/components/notifications/NotificationManagementToolbar.tsx"
      provides: "Tab, type-filter, and visible-read controls"
    - path: "frontend/src/components/notifications/NotificationPreferenceGroups.tsx"
      provides: "Grouped preference UI"
    - path: "frontend/src/api/notificationApi.ts"
      provides: "Frontend client for filtered list and read-visible endpoints"
    - path: "frontend/src/tests/notification-management.test.tsx"
      provides: "Automated proof for synced filters, counts, and read actions"
  key_links:
    - from: "frontend/src/pages/NotificationsPage.tsx"
      to: "frontend/src/api/notificationApi.ts"
      via: "URL-backed tab/type filter requests"
      pattern: "tab|types|readVisible"
    - from: "frontend/src/hooks/useNotifications.ts"
      to: "frontend/src/stores/notificationStore.ts"
      via: "shared unread-count and realtime upsert state"
      pattern: "upsert|setUnreadCount"
    - from: "frontend/src/components/notifications/NotificationDropdown.tsx"
      to: "frontend/src/pages/NotificationsPage.tsx"
      via: "shared preferences and unread counts"
      pattern: "preferences|unread"
---

<objective>
Implement the frontend notification-management center and synchronize it with the existing bell dropdown.

Purpose: Phase 7 turns notifications from a simple list into a management surface with URL-backed filters, grouped preferences, and read actions that stay consistent across dropdown and full-page views.
Output: Updated notification API/store/hooks, page and dropdown UI, and passing frontend tests for P7-04 and P7-05.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-RESEARCH.md
@frontend/src/api/notificationApi.ts
@frontend/src/hooks/useNotifications.ts
@frontend/src/stores/notificationStore.ts
@frontend/src/pages/NotificationsPage.tsx
@frontend/src/components/notifications/NotificationDropdown.tsx
@frontend/src/components/notifications/NotificationList.tsx
@frontend/src/tests/notification-management.test.tsx
@frontend/src/tests/notification-preferences.test.tsx
@.planning/phases/05-notification-detail-actions-quick-notification-settings-and-seller-chat-entry-from-listings/05-02-SUMMARY.md

<interfaces>
Backend notification-management contract from Plan 07-02:
```text
GET /api/notifications?tab=all|unread&types=NEW_MESSAGE,ITEM_SOLD&page=0&size=20
PATCH /api/notifications/read-visible?tab=all|unread&types=NEW_MESSAGE,ITEM_SOLD
```

Existing preference keys:
```ts
newMessageEnabled
itemSoldEnabled
transactionUpdateEnabled
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a URL-backed notifications page with filtered read actions</name>
  <read_first>
    - frontend/src/pages/NotificationsPage.tsx
    - frontend/src/components/notifications/NotificationList.tsx
    - frontend/src/components/notifications/NotificationDropdown.tsx
    - frontend/src/api/notificationApi.ts
    - frontend/src/tests/notification-management.test.tsx
    - .planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-RESEARCH.md
  </read_first>
  <files>frontend/src/api/notificationApi.ts, frontend/src/pages/NotificationsPage.tsx, frontend/src/components/notifications/NotificationList.tsx, frontend/src/components/notifications/NotificationManagementToolbar.tsx, frontend/src/tests/notification-management.test.tsx</files>
  <acceptance_criteria>
    - `notificationApi.ts` contains methods named `getNotifications` that accept `tab` and `types`, and `markVisibleAsRead`.
    - `NotificationsPage.tsx` uses `useSearchParams` and reads the exact keys `tab`, `types`, and `page`.
    - `NotificationManagementToolbar.tsx` exists and contains the exact action copy `Mark visible as read`.
    - `NotificationList.tsx` renders only the currently filtered rows passed from page state instead of fetching its own page-local list.
    - `notification-management.test.tsx` passes.
  </acceptance_criteria>
  <action>Refactor the notifications page into a URL-backed management surface. Update `notificationApi.getNotifications` to accept an object with `tab`, `types`, `page`, and `size`, and add `markVisibleAsRead` that calls `/api/notifications/read-visible` with the same filter params. Move the notifications page to `useSearchParams` with these exact semantics: `tab=all` when omitted, `tab=unread` when the unread tab is selected, `types=NEW_MESSAGE,ITEM_SOLD,TRANSACTION_UPDATE` as a comma-separated filter, and `page` for pagination. Create `NotificationManagementToolbar.tsx` to own the tabs, type filter chips, and `Mark visible as read` action. Change `NotificationList.tsx` from a self-fetching component into a presentational list that receives filtered rows and callbacks from the page.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/tests/notification-management.test.tsx</automated>
  </verify>
  <done>The notifications page becomes a URL-backed management center with filtered reads and no duplicate fetch path inside the list component.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synchronize preferences and unread counts across dropdown and page</name>
  <read_first>
    - frontend/src/hooks/useNotifications.ts
    - frontend/src/stores/notificationStore.ts
    - frontend/src/components/notifications/NotificationDropdown.tsx
    - frontend/src/pages/NotificationsPage.tsx
    - frontend/src/tests/notification-preferences.test.tsx
    - frontend/src/tests/notification-management.test.tsx
  </read_first>
  <files>frontend/src/hooks/useNotifications.ts, frontend/src/stores/notificationStore.ts, frontend/src/pages/NotificationsPage.tsx, frontend/src/components/notifications/NotificationDropdown.tsx, frontend/src/components/notifications/NotificationPreferenceGroups.tsx, frontend/src/tests/notification-management.test.tsx, frontend/src/tests/notification-preferences.test.tsx</files>
  <acceptance_criteria>
    - `NotificationPreferenceGroups.tsx` exists and renders grouped labels `Messages`, `Sales`, and `Transactions`.
    - `useNotifications.ts` updates the shared store through `upsertNotification` or equivalent rather than page-local state.
    - `notificationStore.ts` contains a store action that can reconcile unread counts after `markVisibleAsRead`.
    - `NotificationDropdown.tsx` still shows quick settings, but reads preferences and unread count from the shared store populated by the page or realtime hook.
    - `notification-preferences.test.tsx` and `notification-management.test.tsx` pass together.
  </acceptance_criteria>
  <action>Use the existing Zustand store as the single client-side source of truth for notifications, unread count, and preferences. Update `useNotifications.ts` so realtime messages upsert into the shared store and keep unread count aligned with page and dropdown views. Add `NotificationPreferenceGroups.tsx` to render grouped toggles with the exact groups `Messages`, `Sales`, and `Transactions`; wire those groups to the existing preference keys `newMessageEnabled`, `itemSoldEnabled`, and `transactionUpdateEnabled`. Keep quick settings inside `NotificationDropdown.tsx`, but make the dropdown consume the same store-backed preferences and unread count as the page. Add a store action that applies the effect of `markVisibleAsRead` to the currently filtered IDs so unread totals do not drift between the page and bell dropdown.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/tests/notification-management.test.tsx src/tests/notification-preferences.test.tsx</automated>
  </verify>
  <done>The dropdown and notifications page share one unread-count and preference source of truth, and filtered read actions no longer cause state drift.</done>
</task>

</tasks>

<verification>
Run the notification-focused frontend tests, then spot-check type safety because this plan changes the list component contract and the notification API signatures.
</verification>

<success_criteria>
Users can manage notifications through sharable filtered URLs, grouped preferences, and accurate unread counts across the bell and full notifications page.
</success_criteria>

<output>
After completion, create `.planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-04-SUMMARY.md`
</output>
