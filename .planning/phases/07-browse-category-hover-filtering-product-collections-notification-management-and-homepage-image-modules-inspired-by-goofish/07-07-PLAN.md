---
phase: 07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish
plan: 07
type: execute
wave: 3
depends_on:
  - 07-04
files_modified:
  - frontend/src/hooks/useNotifications.ts
  - frontend/src/stores/notificationStore.ts
  - frontend/src/pages/NotificationsPage.tsx
  - frontend/src/components/notifications/NotificationDropdown.tsx
  - frontend/src/components/notifications/NotificationPreferenceGroups.tsx
  - frontend/src/tests/notification-management.test.tsx
  - frontend/src/tests/notification-preferences.test.tsx
autonomous: true
requirements:
  - P7-04
  - P7-05
must_haves:
  truths:
    - "Unread counts and read actions stay synchronized between the bell dropdown and the notification page."
    - "Grouped preferences render with the same source of truth across dropdown and page."
    - "Realtime notification updates flow through one shared store instead of drifting between surfaces."
  artifacts:
    - path: "frontend/src/hooks/useNotifications.ts"
      provides: "Shared realtime notification hydration into the store"
    - path: "frontend/src/stores/notificationStore.ts"
      provides: "Shared unread-count and visible-read reconciliation logic"
    - path: "frontend/src/components/notifications/NotificationDropdown.tsx"
      provides: "Dropdown quick settings and unread-count parity"
    - path: "frontend/src/components/notifications/NotificationPreferenceGroups.tsx"
      provides: "Grouped preference UI"
    - path: "frontend/src/tests/notification-preferences.test.tsx"
      provides: "Automated proof for grouped preferences and cross-surface parity"
  key_links:
    - from: "frontend/src/hooks/useNotifications.ts"
      to: "frontend/src/stores/notificationStore.ts"
      via: "shared unread-count and realtime upsert state"
      pattern: "upsert|setUnreadCount"
    - from: "frontend/src/components/notifications/NotificationDropdown.tsx"
      to: "frontend/src/pages/NotificationsPage.tsx"
      via: "shared preferences and unread counts"
      pattern: "preferences|unread"
---

<objective>
Synchronize dropdown, store, and grouped preference behavior after the page-level notification management contract is in place.

Purpose: This follow-on plan isolates the shared-state work so dropdown parity, realtime hydration, and grouped preferences can be implemented without competing with URL-state page refactors.
Output: Shared notification store synchronization, grouped preference UI, dropdown parity, and passing frontend synchronization tests.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-RESEARCH.md
@frontend/src/hooks/useNotifications.ts
@frontend/src/stores/notificationStore.ts
@frontend/src/pages/NotificationsPage.tsx
@frontend/src/components/notifications/NotificationDropdown.tsx
@frontend/src/tests/notification-management.test.tsx
@frontend/src/tests/notification-preferences.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Synchronize shared notification store and unread-count parity across page and dropdown</name>
  <read_first>
    - frontend/src/hooks/useNotifications.ts
    - frontend/src/stores/notificationStore.ts
    - frontend/src/components/notifications/NotificationDropdown.tsx
    - frontend/src/pages/NotificationsPage.tsx
    - frontend/src/tests/notification-management.test.tsx
  </read_first>
  <files>frontend/src/hooks/useNotifications.ts, frontend/src/stores/notificationStore.ts, frontend/src/pages/NotificationsPage.tsx, frontend/src/components/notifications/NotificationDropdown.tsx, frontend/src/tests/notification-management.test.tsx</files>
  <acceptance_criteria>
    - `useNotifications.ts` updates the shared store through `upsertNotification` or equivalent rather than page-local state.
    - `notificationStore.ts` contains a store action that can reconcile unread counts after `markVisibleAsRead`.
    - `NotificationDropdown.tsx` reads unread count from the shared store populated by the page or realtime hook.
    - `notification-management.test.tsx` passes together with store/dropdown parity assertions.
    - This task does not create grouped preference UI files.
  </acceptance_criteria>
  <action>Use the existing Zustand store as the single client-side source of truth for notifications and unread count. Update `useNotifications.ts` so realtime messages upsert into the shared store and keep unread count aligned with page and dropdown views. Update `NotificationDropdown.tsx` so it consumes the same store-backed unread count and notification state as the page. Add a store action that applies the effect of `markVisibleAsRead` to the currently filtered IDs so unread totals do not drift between the page and bell dropdown. Leave grouped preference UI to the next task.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/tests/notification-management.test.tsx</automated>
  </verify>
  <done>The dropdown and notification page share one unread-count source of truth with realtime and visible-read reconciliation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add grouped preference parity for page and dropdown</name>
  <read_first>
    - frontend/src/stores/notificationStore.ts
    - frontend/src/components/notifications/NotificationDropdown.tsx
    - frontend/src/pages/NotificationsPage.tsx
    - frontend/src/tests/notification-preferences.test.tsx
    - frontend/src/tests/notification-management.test.tsx
  </read_first>
  <files>frontend/src/pages/NotificationsPage.tsx, frontend/src/components/notifications/NotificationDropdown.tsx, frontend/src/components/notifications/NotificationPreferenceGroups.tsx, frontend/src/tests/notification-preferences.test.tsx</files>
  <acceptance_criteria>
    - `NotificationPreferenceGroups.tsx` exists and renders grouped labels `Messages`, `Sales`, and `Transactions`.
    - `NotificationDropdown.tsx` reads preferences from the shared store and preserves quick settings.
    - `NotificationsPage.tsx` and `NotificationDropdown.tsx` use the same grouped preference component or equivalent shared contract.
    - `notification-preferences.test.tsx` passes.
    - Grouped preferences still map only to `newMessageEnabled`, `itemSoldEnabled`, and `transactionUpdateEnabled`.
  </acceptance_criteria>
  <action>Add `NotificationPreferenceGroups.tsx` to render grouped toggles with the exact groups `Messages`, `Sales`, and `Transactions`; wire those groups to the existing preference keys `newMessageEnabled`, `itemSoldEnabled`, and `transactionUpdateEnabled`. Keep quick settings inside `NotificationDropdown.tsx`, but make the dropdown consume the same store-backed preferences and grouped contract as the page. Do not add new preference keys or new notification semantics.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/tests/notification-preferences.test.tsx src/tests/notification-management.test.tsx</automated>
  </verify>
  <done>Grouped preferences render consistently across dropdown and page without introducing a second preference source of truth.</done>
</task>

</tasks>

<verification>
Run the notification synchronization and preference slices only; the broader verification gate remains in `07-05`.
</verification>

<success_criteria>
Notification dropdown parity, realtime store synchronization, and grouped preferences are implemented as a dedicated follow-on step after page/filter work.
</success_criteria>

<output>
After completion, create `.planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-07-SUMMARY.md`
</output>
