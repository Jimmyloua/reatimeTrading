---
phase: 07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish
plan: 03
type: execute
wave: 3
depends_on:
  - 07-00
  - 07-06
files_modified:
  - frontend/src/App.tsx
  - frontend/src/pages/HomePage.tsx
  - frontend/src/pages/BrowseListingsPage.tsx
  - frontend/src/components/browse/BrowseCategoryDisclosure.tsx
  - frontend/src/components/browse/CuratedCollectionRail.tsx
  - frontend/src/components/home/HomepageModuleRenderer.tsx
  - frontend/src/tests/browse-category-hover.test.tsx
  - frontend/src/tests/homepage-modules.test.tsx
autonomous: true
requirements:
  - P7-01
  - P7-02
  - P7-03
must_haves:
  truths:
    - "Browse category navigation previews categories locally and only commits filters when the user explicitly applies one."
    - "Homepage modules render from backend payloads instead of hardcoded JSX."
    - "Category, collection, and homepage module entry points route into sharable browse URLs."
  artifacts:
    - path: "frontend/src/pages/HomePage.tsx"
      provides: "Dedicated server-driven homepage page"
    - path: "frontend/src/components/home/HomepageModuleRenderer.tsx"
      provides: "Ordered rendering for hero, image tile, collection row, and category spotlight modules"
    - path: "frontend/src/components/browse/BrowseCategoryDisclosure.tsx"
      provides: "Accessible category disclosure with hover enhancement"
    - path: "frontend/src/api/contentApi.ts"
      provides: "Frontend client for /api/content/homepage and /api/content/collections/{slug}"
    - path: "frontend/src/tests/browse-category-hover.test.tsx"
      provides: "Automated proof for preview versus committed browse state"
  key_links:
    - from: "frontend/src/pages/HomePage.tsx"
      to: "frontend/src/api/contentApi.ts"
      via: "homepage query"
      pattern: "getHomepage"
    - from: "frontend/src/pages/BrowseListingsPage.tsx"
      to: "frontend/src/components/browse/BrowseCategoryDisclosure.tsx"
      via: "local preview and committed category filter"
      pattern: "hovered|preview|categoryId"
    - from: "frontend/src/pages/BrowseListingsPage.tsx"
      to: "frontend/src/api/contentApi.ts"
      via: "collection query-param hydration"
      pattern: "collection"
---

<objective>
Implement the frontend browse and homepage experiences for Phase 7 using the new content API and URL-backed browse state.

Purpose: This plan delivers the user-facing merchandising surfaces: accessible category disclosure, curated collection context, and server-driven homepage modules. It keeps the canonical state in the URL while leaving hover preview local.
Output: Dedicated `HomePage`, browse disclosure and collection UI, content API/types, and passing frontend tests for P7-01 through P7-03.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-RESEARCH.md
@frontend/src/App.tsx
@frontend/src/pages/BrowseListingsPage.tsx
@frontend/src/components/ListingFilters.tsx
@frontend/src/api/listingApi.ts
@frontend/src/types/listing.ts
@frontend/src/tests/browse-category-hover.test.tsx
@frontend/src/tests/homepage-modules.test.tsx
@.planning/phases/05-notification-detail-actions-quick-notification-settings-and-seller-chat-entry-from-listings/05-02-SUMMARY.md

<interfaces>
Backend content endpoints and frontend contracts from Plan 07-06:
```text
GET /api/content/homepage
GET /api/content/collections/{slug}
```

Browse params to preserve:
```text
query, categoryId, minPrice, maxPrice, conditions, city, region, page, sort
```

New Phase 7 browse param:
```text
collection={slug}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace hardcoded home markup with server-driven homepage modules</name>
  <read_first>
    - frontend/src/App.tsx
    - frontend/src/tests/homepage-modules.test.tsx
    - frontend/src/api/listingApi.ts
    - .planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-RESEARCH.md
    - .planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-VALIDATION.md
  </read_first>
  <files>frontend/src/App.tsx, frontend/src/pages/HomePage.tsx, frontend/src/components/home/HomepageModuleRenderer.tsx, frontend/src/components/browse/CuratedCollectionRail.tsx, frontend/src/tests/homepage-modules.test.tsx</files>
  <acceptance_criteria>
    - `frontend/src/pages/HomePage.tsx` exists and uses a query against `/api/content/homepage`.
    - `frontend/src/types/content.ts` contains module types `hero`, `image_tiles`, `collection_row`, and `category_spotlight`.
    - `HomepageModuleRenderer.tsx` contains routing branches for `category`, `collection`, `listing`, and `route`.
    - `App.tsx` imports `HomePage` from `frontend/src/pages/HomePage.tsx` instead of keeping the homepage inline.
    - `homepage-modules.test.tsx` passes.
  </acceptance_criteria>
  <action>Extract the inline `HomePage` component from `App.tsx` into `frontend/src/pages/HomePage.tsx` and replace the hardcoded cards with a query-driven page built on the `contentApi.getHomepage()` client created in Plan `07-06`. Build `HomepageModuleRenderer.tsx` to render the four backend module types exactly: `hero`, `image_tiles`, `collection_row`, and `category_spotlight`. Route module items by `linkType` and `linkValue` with these exact mappings: `category` -> `/listings?categoryId={value}`, `collection` -> `/listings?collection={value}`, `listing` -> `/listings/{value}`, `route` -> the raw route value. Reuse the current visual language from the existing home hero rather than inventing a new design system.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/tests/homepage-modules.test.tsx</automated>
  </verify>
  <done>The homepage becomes server-driven, module-ordered, and wired to the content backend while staying within the existing visual language.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add accessible category disclosure and collection-aware browse routing</name>
  <read_first>
    - frontend/src/pages/BrowseListingsPage.tsx
    - frontend/src/components/ListingFilters.tsx
    - frontend/src/types/listing.ts
    - frontend/src/tests/browse-category-hover.test.tsx
    - frontend/src/pages/HomePage.tsx
    - frontend/src/api/contentApi.ts
  </read_first>
  <files>frontend/src/pages/BrowseListingsPage.tsx, frontend/src/components/browse/BrowseCategoryDisclosure.tsx, frontend/src/components/browse/CuratedCollectionRail.tsx, frontend/src/tests/browse-category-hover.test.tsx</files>
  <acceptance_criteria>
    - `BrowseListingsPage.tsx` reads and writes the `collection` query param in addition to the existing browse params.
    - `BrowseCategoryDisclosure.tsx` contains local state for preview or open category behavior and does not write search params on hover-only events.
    - `BrowseCategoryDisclosure.tsx` contains the exact button or label text `Browse categories`.
    - `BrowseListingsPage.tsx` uses TanStack Query `placeholderData` for the listings query.
    - `browse-category-hover.test.tsx` passes.
  </acceptance_criteria>
  <action>Implement a new `BrowseCategoryDisclosure.tsx` component that uses semantic buttons and links, not ARIA menu roles, to expose category groups with hover enhancement and click/focus disclosure behavior. Keep transient preview state local in that component and commit browse filters only through explicit user actions that set the URL param `categoryId`. Extend `BrowseListingsPage.tsx` to preserve a new `collection` search param, fetch `contentApi.getCollection(collectionSlug)` when present, and render a `CuratedCollectionRail` section above the listing grid using the ordered collection items. Keep existing listing search params intact, reset `page` to `0` on filter changes, and update the listings query to use `placeholderData: (previous) => previous` so result grids do not blank between route-backed filter changes.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/tests/browse-category-hover.test.tsx src/tests/homepage-modules.test.tsx</automated>
  </verify>
  <done>Browse supports local category preview, explicit filter commits, and sharable collection-aware URLs that pair correctly with homepage entry points.</done>
</task>

</tasks>

<verification>
Run the focused homepage and browse tests, then do a local frontend build if type or router changes touch shared app wiring.
</verification>

<success_criteria>
Users can enter browse through homepage modules or category disclosure without losing sharable URL state, and homepage merchandising is fully server-driven.
</success_criteria>

<output>
After completion, create `.planning/phases/07-browse-category-hover-filtering-product-collections-notification-management-and-homepage-image-modules-inspired-by-goofish/07-03-SUMMARY.md`
</output>
