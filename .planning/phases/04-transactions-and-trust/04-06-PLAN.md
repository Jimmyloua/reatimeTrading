---
phase: 04-transactions-and-trust
plan: 06
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05]
files_modified: []
autonomous: false
requirements: [TRAN-01, TRAN-02, TRAN-03, TRAN-04, TRAN-05, TRAN-06, RATE-01, RATE-02, RATE-03, RATE-04]
must_haves:
  truths:
    - "All backend tests pass"
    - "Frontend builds successfully"
    - "User can complete full transaction workflow"
    - "User can rate and see ratings"
  artifacts:
    - path: "backend/target/surefire-reports"
      provides: "Test results"
    - path: "frontend/dist"
      provides: "Production build"
  key_links: []
---

<objective>
Verify all Phase 4 functionality through automated tests and manual verification.

Purpose: Ensure all requirements are met before marking phase complete.
Output: Verification results, phase summary.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-transactions-and-trust/04-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run all backend tests</name>
  <files>
    backend/target/surefire-reports
  </files>
  <read_first>
    backend/src/test/java/com/tradingplatform/transaction/service/TransactionServiceTest.java
    backend/src/test/java/com/tradingplatform/transaction/service/RatingServiceTest.java
  </read_first>
  <action>
Run all backend tests and ensure they pass:

```bash
cd backend && mvn test -q
```

If tests fail, investigate and fix issues. Common issues:
- Missing entity relationships
- Incorrect state machine transitions
- Missing error codes

Verify test coverage for:
- TRAN-01: TransactionServiceTest#testCreateTransaction
- TRAN-02: TransactionServiceTest#testGetTransactionHistory
- TRAN-03: TransactionServiceTest#testStatusTransitions
- TRAN-04: RatingServiceTest#testSubmitRating
- TRAN-05: RatingServiceTest#testBidirectionalRating
- TRAN-06: RatingServiceTest#testReviewText
- RATE-01: RatingServiceTest#testRatingValidation
- RATE-02: UserServiceTest#testRatingDisplay
- RATE-03: RatingServiceTest#testAggregateRating
- RATE-04: RatingServiceTest#testRatingCount
  </action>
  <verify>
    <automated>cd backend && mvn test -q 2>&1 | grep -E "(Tests run|FAILURE|SUCCESS)"</automated>
  </verify>
  <done>
    - All tests pass (mvn test exits with code 0)
    - No test failures in surefire reports
    - Coverage for all 10 requirements
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run frontend build and type check</name>
  <files>
    frontend/dist
  </files>
  <read_first>
    frontend/src/App.tsx
    frontend/src/pages/TransactionsPage.tsx
    frontend/src/pages/TransactionDetailPage.tsx
  </read_first>
  <action>
Run frontend build and type check:

```bash
cd frontend && npm run build
```

Verify:
- TypeScript compiles without errors
- Vite build succeeds
- No missing imports or type errors

Run type check:
```bash
cd frontend && npx tsc --noEmit
```
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Frontend build succeeds
    - dist folder contains production assets
    - No TypeScript errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 4 complete implementation:
- Transaction backend with state machine (CREATED -> FUNDED -> RESERVED -> DELIVERED -> CONFIRMED -> SETTLED -> COMPLETED)
- Rating backend with blind rating system
- Dispute workflow for conflict resolution
- Frontend transaction list and detail pages
- Rating form and profile rating display
</what-built>
  <how-to-verify>
**1. Start the backend:**
```bash
cd backend && mvn spring-boot:run
```

**2. Start the frontend:**
```bash
cd frontend && npm run dev
```

**3. Test Transaction Workflow:**
1. Register/login as User A
2. Create a listing as User A
3. Register/login as User B
4. View the listing and click "Request to Buy"
5. Verify transaction created with CREATED status
6. As User A (seller), accept the request
7. As User B (buyer), confirm payment sent
8. As User A, confirm funds received
9. As User A, mark as shipped
10. As User B, confirm receipt
11. Verify transaction reaches COMPLETED status

**4. Test Rating Workflow:**
1. After transaction completes, verify "Leave a Review" button appears
2. As User B, submit a 5-star rating with review text
3. Verify "pending" message shows (blind rating)
4. As User A, submit a 4-star rating
5. Verify both ratings become visible
6. Check User A and User B profiles show updated ratings

**5. Test Transaction History:**
1. Navigate to /transactions
2. Verify "Purchases" and "Sales" tabs work
3. Verify transactions appear in correct tabs
4. Verify status filters work

**6. Test Profile Rating Display:**
1. Navigate to user profile
2. Verify average rating and count display
3. Verify recent reviews show
</how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. All backend tests pass
2. Frontend build succeeds
3. Manual verification of transaction workflow
4. Manual verification of rating workflow
</verification>

<success_criteria>
- All automated tests pass
- Frontend builds successfully
- User can complete full transaction lifecycle
- User can submit and view ratings
- Profile shows rating summary
</success_criteria>

<output>
After completion, create `.planning/phases/04-transactions-and-trust/04-06-SUMMARY.md`
</output>